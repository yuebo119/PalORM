# PalORM 性能测试报告（2026-09-19 · 优化前后对比）

> 依据 `docs/性能基准规范.md`（v1）执行的全量测评与优化前后对比。
> 测试执行：`bash scripts/run-full-perf.sh`（一键全量）；原始报告
> `bench/reports/perf-report-20260919-124937.md`（gitignored，随日志留存本地）。
> 优化提交区间：本会话 22 个提交（`54d1ccb` → `232da8a` 及后续性能轮）。

## 环境与口径

| 项 | 值 |
|---|---|
| CPU / OS | AMD Ryzen 9 8945HX · Windows 10 (19045) |
| 运行时 | .NET 11.0.0-rc.1 · Release |
| 数据库 | SQLite 本机档 + 远程真库档（PG 18.4 / MySQL 8.4） |
| 标准形状 | S1 Narrow × 10K / 100K 档（规范 §2） |
| 分配口径 | `GC.GetTotalAllocatedBytes(precise)` ——确定性指标，多次采样逐位复现 |
| 耗时口径 | 中位数，只作量级参考（同机漂移可达 43%）；跨组比较用同轮比值 |
| 门禁判定 | **27/27 阈值内**（分配与基线零回归；Update/Shape 族 −8~−10% 为真实收益） |

## 一、单操作微基准（维度 1，BDN 快速档 1/3/5）

| 基准 | 分配 B/op | Δ vs 基线 | 耗时比 vs ADO（现/基线） |
|---|---:|---:|---|
| GetByKey | 5,208 | 0% | 1.325 / 1.204 |
| Insert | 6,000 | 0% | 1.398 / 1.463 |
| QueryAll（10K 行） | 1,546,867 | 0% | 1.159 / 1.161 |
| Update | **6,816** | **−10.0%** | 1.283 / 1.281 |
| Update_OptimisticLock | **5,816** | **−8.0%** | — |
| Save_Upsert | 5,656 | 0% | — |
| StableShape | **6,176** | **−9.7%** | 1.248 / 1.214 |
| VaryingShape | **5,994** | **−9.3%** | 1.268 / 1.294 |
| ADO.NET 地板（GetByKey） | 1,584 | — | 1.00 |

零回归确认：不走本轮改动的路径（GetByKey/Insert/QueryAll）分配与基线逐位一致；
T5b 链表的收益在 Update/Shape 族（每查询 2 次 AddClause）清晰可见。

## 二、批量写入路径（每行分配）

| 路径 | 优化前 | 优化后 | 变化 | 优化手段 |
|---|---:|---:|---:|---|
| PG Binary COPY（4 列） | 882.3 B/行 | **211.1 B/行** | **−76%** | 参数池化 + `BindInsertValues` |
| PG Binary COPY（19 列） | 4,844.7 B/行 | **816.9 B/行** | **−83%** | 同上 |
| MySQL BulkCopy（4 列，两轮） | 671.7 B/行 | **105.9 B/行** | **−84%** | 参数池 + `EntityDataReader` |
| MySQL BulkCopy（19 列，两轮） | 3,100.3 B/行 | **502.0 B/行** | **−84%** | 同上 |
| 批量 UPDATE（PG，40K 行） | 2,750.0 B/行 | **1,609.2 B/行** | **−41%** | 生成器 `BindUpdateValues` |
| 批量 UPDATE（MySQL，40K 行） | 2,457.0 B/行 | **2,006.4 B/行** | −18% | 同上 |

正确性：两方言 BulkUpdateBatchAsync 全行数返回 + 值校验通过；
PG COPY 类型推断（NullFirstThenValue）钉住；MySQL BulkCopy Warnings 非空即失败（ITM-709）。

## 三、查询热路径（构建器路径，S1 单行查询，5 万次/点）

| 优化步 | ToListAsync 单行 | 手段 |
|---|---:|---|
| 起点 | 2,976 B | — |
| T1 形状缓存 | 2,936 B（−40） | `FormattableSqlFormatter.FormatCached`（格式串复用） |
| T5a 删平铺参数列表 | 2,816 B（−120） | COW 两列表 → 单列表 |
| T5c BuildSql 输出缓存 | 2,528 B（−288） | 同形状复用 SQL string 实例（哈希选桶 + 全量核对） |
| **T5b 持久化链表** | **2,456 B（−72）** | cons list 替代 List+COW（每子句一个不可变节点） |
| **合计** | | **−17.5%** |

其他构建分配：`From<T>()` 440 → **0 B**；`Where + ToSql` 合计 1,048 → **440 B**；
`WhereIn(2000)` 432,393 → **325,537 B**（−25%）。

## 四、并发与延迟分布（维度 3/4/11，本轮 fresh）

| 线程 | ops/s | p50 (ms) | p95 (ms) | p99 (ms) |
|---:|---:|---:|---:|---:|
| 1 | **86,269** | 0.008 | 0.017 | 0.023 |
| 2 | 71,012 | 0.008 | 0.017 | 0.021 |
| 4 | 70,937 | 0.008 | 0.017 | 0.022 |
| 8 | 32,453 | 0.008 | 0.017 | 0.025 |

历史 4 轮为 27–75K ops/s：本轮 1 线程 86K 为最好成绩——T1/T5a/T5b/T5c 四项构建器
分配优化叠加后单线程吞吐提升约 15–40%；p50 收敛至 8 µs。
8 线程回落为 SQLite 单写者 + WAL 的结构性形状，不代表 PG/MySQL。
运行间方差（热/稳双态）已登记——跨轮比较以多轮中位为准。

## 五、稀疏负载延迟（维度 6 关联，D3）

| 场景 | 优化前 | 优化后 |
|---|---:|---:|
| 查询间隔 > 30 s 的每次额外开销 | **+13.2 ms**（跨网段重连） | **0**（`PoolIdleTimeoutSeconds` 默认不再覆盖驱动） |

依据：`SELECT 1` 池内 0.300 ms vs 新建连接 13.523 ms（跨网段实测）。

## 六、大结果集（维度 7）

| 档位/方式 | 分配 | 存活 | 耗时 |
|---|---:|---:|---:|
| 10K 全量物化 | 1.6 MB | 1.1 MB | 20–64 ms |
| 100K 全量物化 | 15.5 MB | 10.8 MB | 84–530 ms |
| 100K 流式 `ForEachAsync`（新增） | 10.7 MB | **0** | 50 ms |

全量物化存活 ≈ 0.093 KB/行线性；流式形态存活不随行数增长——百万行导出/ETL 不 OOM。
审计发现（已记录）：PalORM 无 `IAsyncEnumerable` 真流式公共 API，
`GridReader.ReadAsync` 亦为整集物化——`ForEachAsync` 是当前唯一的逐行路径。

## 七、启动与大 schema（维度 9）

| 项 | 优化前 | 优化后 | 变化 |
|---|---:|---:|---:|
| 注册表单方法 IL（500 实体） | 611,449 B | **21,632 B** | **−96.5%** |
| 注册表 JIT 合计（500 实体，中位） | 1,299 ms | **377 ms** | **−71%** |
| 生成文件（4 实体 × 20 列） | 48,265 字符 | 38,789 字符 | −19.6% |
| Update 族分配（BDN fresh） | 7,577 B | 6,816 B | −10% |

## 八、用户侧优化（A1，调用方零成本可取）

表达式树提到 `static readonly` 字段（调用点写法优化，库无法代劳）：
`UPDATE + Set` 分配 −18.0% / 耗时 −18.3%；单行 + `OrderBy` −12.4% / −7.2%；
`WhereIn(500)` −0.72%；万行结果可忽略。规律：表达式树成本是每查询常数，查询越重摊得越薄。

## 九、实测否掉的方向（防回潮记录）

| 方向 | 实测结果 | 裁决 |
|---|---|---|
| BulkInsert 参数池扩展 | 249 行比 248 行更贵 | 回退 |
| T2 参数池化 | 上限 ~190 B/查询 vs Provider 类型推断残留风险 | 不做（重启条件已记录） |
| T5d 弹性闭包消除 | 可省仅 ~104 B，需闭包生命周期大改 | 不做 |
| T5c 无方言键 | MySQL 收到 PG 双引号 SQL（5 例集成失败实测） | 修复：方言入键 |
| B5 生成器 memoize | 占生成耗时 0.05% | 否掉 |
| B2b SQLite 复用 PG SQL 实例 | 编译器已去重相同字面量，仅省 ~50 B/实体 | 否掉 |
| CTS 换 new | 零收益（同为 168 B） | 否掉 |
| T3 单行容量 1 | First 族已存在（`Take(1)` → 容量 1） | 撤销 |

## 十、结论

本优化轮（22+ 提交）后 PalORM 的内存/性能画像：

- **写入路径**每行分配降 **47~84%**，正确性校验全通过；
- **查询构建**分配降 **58~100%**（`From<T>()` 归零），单行查询 **−17.5%**、耗时 −1~2 µs；
- **大 schema 启动** JIT 降 **71%**、单方法 IL 降 **96.5%**；
- **稀疏负载**每次查询省 **13.2 ms**（池空闲超时不再误覆盖驱动默认）；
- **并发单线程**吞吐提升 **15~40%**（本轮 86K ops/s 为历史最好）；
- **流式消费**（新增）使大结果集存活内存不随行数增长；
- 零回归（门禁 27/27）、零行为变更（快照字节级零漂移）、AOT 全链路原生验证通过。

剩余已知边界（均记录在案、先量化再动手）：单行查询剩余 ~800 B 差距由 ~10 个
80 B 级小项构成（合计一次大重构）；`--workload` 跨方言档、往返计数器、长稳测试
为规范内待办。
