# PalORM v4.0 极致改进方案

> 基于 4 路并行深度调研（核心架构 / 源生成器 / 性能 hot path / API 设计）整合产出。
>
> **目标**：在 v3.1 已完成的 IRowFactory 委托化、Converter 单例、快照合并基础上，再榨取 30-50% 性能、消除 API 残留、收口源生成器 emit 不对称、完成 AOT 全链路最后 1 公里。
>
> **状态：📋 待评审**（2026-07-21）。本方案不立即实施，供评审通过后在新会话执行。

---

## 一、调研发现的全景问题

### 1.1 性能维度（12 个零分配机会）

| # | 问题 | file:line | 收益 | 风险 |
|---|------|-----------|------|------|
| P1 | GetAsync 未合并 Volatile.Read（From<T> 已做，CRUD 未对齐） | `DataSession.Crud.cs:256-258` | ~30ns + 2 次 fence/查询 | 低 |
| P2 | GetAsync 每次重建 SELECT 列表 `string.Join(", ", cols.Select(Quote))` | `DataSession.Crud.cs:263,285` | ~150ns + 1 次字符串分配/查询 | 低 |
| P3 | GetAsync `$"SELECT ..."` 插值构建 SQL | `DataSession.Crud.cs:264` | ~80ns + 1 次分配/查询 | 低 |
| P4 | **CommandFactoryEmitter Bind 路径未对齐 Converter 单例** | `CommandFactoryEmitter.cs:95,289` | **每写操作每 Converter 列省 1 次 Gen0 分配** | 低 |
| P5 | BindInsert 每 col 调 `cmd.CreateParameter()` + `Parameters.Add` | `CommandFactoryEmitter.cs:251` | 10-30ns/参数 | 中 |
| P6 | `ToListAsync` 命中/未命中均 `new List<T>(cached)` 浅拷贝 | `QueryBuilderExtensions.cs:19,68` | 1 次 List+数组分配/查询 | 中（语义） |
| P7 | `List<T>` 默认 Capacity=0，10K 行扩容多次 | `QueryBuilderExtensions.cs:64` | ~3 次 Gen0/查询 | 低 |
| P8 | `AddClause` 写时复制 `new List<>(_clauses)` 每次 O(n) | `QueryBuilder.cs:705-706` | 链式构建 O(N²)→O(N) | 高（破坏 struct 副本隔离） |
| P9 | `HasClause` 用 `_clauses.Exists(pred)` O(n) 扫描 | `QueryBuilder.cs:712-713` | O(N)→O(1)（bitmask） | 中 |
| P10 | Tracing/Metrics 默认 false 时仍分配 `operation`/`provider` 串 | `QueryBuilderExtensions.cs:41-48` | ~50ns/查询（默认场景） | 低 |
| P11 | 租户过滤每次 `FormattableStringFactory.Create` | `DataSession.Crud.cs:50` | ~40ns + 1 分配/查询 | 低 |
| P12 | Metrics Counter.Add 无 listener 时仍走完整路径 | `QueryBuilderExtensions.cs:83` | ~80ns/查询（默认场景） | 低 |

### 1.2 架构维度（8 个改进点）

| # | 问题 | 改进方向 |
|---|------|----------|
| A1 | QueryBuilder struct + 写时复制 → 链式 API O(N²) 拷贝 | 改 class builder 或 lazy copy 标志位 |
| A2 | `CrudMetadata.RowFactory` 仍 `object` + 5 处 castclass | 改 `Delegate` 类型或 `Func<DbDataReader, object>` |
| A3 | `PalORM_Runtime` 16 个属性各自 Volatile.Read | CRUD 路径全部用 `CurrentState` 快照（From<T> 模式推广） |
| A4 | DataSession ctor 内 `interceptors.OrderBy().ToList()` 每会话一次 | 在 `DbOptions.Build` 阶段冻结，会话取引用 |
| A5 | 13+ 处 `cmd.CommandTimeout = _options.CommandTimeoutSeconds` 重复 | 信任工厂，删除重复赋值 + 加测试守卫 |
| A6 | QueryCache 失效策略保守（无写入后主动失效） | 增加 `InvalidateOnWrite<T>` 钩子或明确"调用方负责"契约 |
| A7 | `IRowFactory<T>` 仍 public 但运行时不调用 | XML doc 已说明；v4.0 评估真外部使用后决定 `[Obsolete]` |
| A8 | `RowFactories` 字典值 `object` → 装箱 + castclass | 改 `Dictionary<Type, Delegate>` |

### 1.3 源生成器维度（6 个 emit 优化）

| # | 问题 | 改进方向 |
|---|------|----------|
| S1 | **CommandFactoryEmitter Bind 路径每行 new Converter()**（与 RowFactoryEmitter 不对称） | 对齐 RowFactory 模式：类级 `static readonly _conv_cmd_{prop}` 字段 |
| S2 | 所有 Emitter 都 `new StringBuilder()` 后 `sb.ToString()` | 改 `StringBuilderPool.Rent()` + `AddSource(hint, SourceText)` |
| S3 | RowFactoryEmitter + CommandFactoryEmitter 可合并为 `EntityFactory_{suffix}` | 每实体省 ~12 行（auto-generated 头 + namespace + class 包裹） |
| S4 | MigrationEmitter legacy `CreateTable` 与 `CreateTableSqlite` 重复 | 删 legacy 字段（dialect 字典恒有 Sqlite 键，不可达路径） |
| S5 | CommandFactoryEmitter `BindUpsert = BindInsert + PK` 重复 emit 循环 | 改为调用 BindInsert 后追加 PK 行 |
| S6 | PALORM002 纯风格警告、PALORM004 在 FK DDL 永不启用前无价值 | 降级为 Suggestion 或与 PALORM017 合并 |

### 1.4 API 维度（7 个收口项）

| # | 问题 | 改进方向 |
|---|------|----------|
| API1 | `DiffAsync` 是 `ValidateSchemaAsync` 的薄包装 | deprecate 或合并 |
| API2 | `[Column].Length/Precision/Scale/TypeName/StoreAs` 未实现但 public | 标 `[Obsolete("ITM-549 未实现")]` 或 PALORM017 升级为 Error |
| API3 | `NamingConvention.ApplyNaming` 运行时选项对编译期映射无效 | deprecate 或迁移为编译期注解 |
| API4 | `WithMetrics(name)` name 参数仅校验不作为标签 | 删除参数或真正用作标签 |
| API5 | `GetRawConnection()` 逃生舱命名不够警示 | 重命名 `DangerousGetRawConnection` |
| API6 | `IRowFactory<T>` 接口文档与代码不一致 | 明确收口（见 A7） |
| API7 | `BulkInsertAsync` 参数顺序在 DataSession 层 vs Provider 层不同 | 文档说明职责差异 |

### 1.5 AOT 维度（3 个最后 1 公里）

| # | 问题 | 改进方向 |
|---|------|----------|
| T1 | `Convert.ChangeType` 在 Max/Min/Scalar 路径 | 对枚举/Guid 不安全；源生成特化或禁用这些类型 |
| T2 | OwnedJson 在 AOT 下需强制 JsonSerializerContext | 已实现，但 AotTest 未覆盖枚举/Guid 列 |
| T3 | 三方言原生 lib 的 RID 解析 | CI 已验证，但本地 publish 指南缺失 |

---

## 二、核心改进方案（按收益/风险排序）

### 🥇 优化 A：CommandFactory Converter 单例对齐（P4 + S1）

**这是 v3.1 最大遗留不对称**——RowFactory 已用单例，CommandFactory 仍每次 new。

**预期收益**：百万行 BulkInsert 场景，含 2 个 Converter 列的实体省 **~200 万次 Gen0 分配**。

**改动**：

| 文件 | 改动 |
|------|------|
| `src/PalORM.SourceGen/CommandFactoryEmitter.cs:33`（新增） | emit 类级 `private static readonly IValueConverter<TClr,TProv> _conv_cmd_{prop} = new Converter();` |
| `src/PalORM.SourceGen/CommandFactoryEmitter.cs:95` | BindDelete body 中 `new {ConverterTypeName}()` → `_conv_cmd_{prop}!.ToProvider(...)` |
| `src/PalORM.SourceGen/CommandFactoryEmitter.cs:289` | GetParameterValueExpression 中同理 |
| 快照基线 | `PALORM_UPDATE_SNAPSHOTS=1 dotnet run --project test/PalORM.SourceGen.Tests` 重生成 |

**风险**：低——emit 内局部变更，RowFactory 已验证模式直接复用。

---

### 🥈 优化 B：CRUD 路径 Volatile.Read 合并 + 预构建 SQL（P1 + P2 + P3 + A3）

**问题**：From<T> 已用 `PalORM_Runtime.CurrentState` 单次快照（v3.1 优化 3a），但 CRUD 路径（GetAsync/GetAllAsync/InsertAsync/UpdateAsync/DeleteAsync）仍 3 次独立访问。

**预期收益**：GetByKey 再降 ~10%（从 31μs → ~28μs），贴近 ADO.NET 基线。

**改动**：

| 文件 | 改动 |
|------|------|
| `src/PalORM.Core/DataSession.Crud.cs:255-272`（GetAsync） | 改用 `var s = PalORM_Runtime.CurrentState;` + `s.RowFactories/...`；移除独立属性访问 |
| `src/PalORM.Core/DataSession.Crud.cs:278-292`（GetAllAsync） | 同理 |
| `src/PalORM.Core/CrudMetadata.cs`（新增字段） | `QuotedColumnList` 预构建 `string.Join(", ", cols.Select(Quote))` 结果 |
| `src/PalORM.SourceGen/RegistryEmitter.cs` | emit 时为每个实体计算 `QuotedColumnList`（需 Provider quote 规则——但 emit 时不知道 Provider，方案：emit 三方言各一份 + 运行时按 dialect 选择） |

**实施陷阱**：`QuotedColumnList` 依赖 Provider 的 `QuoteIdentifier`，但 emit 时是 Provider-agnostic。**解决方案**：emit 三方言各一份（`QuotedColumnList_Sqlite/PostgreSql/MySql`），运行时按 `TProvider.Dialect` 选择。这与 MigrationEmitter 三方言 emit 模式一致。

**风险**：中——需在 CrudMetadata 新增字段 + RegistryEmitter emit 三方言。但模式已验证（MigrationEmitter 已做）。

---

### 🥉 优化 C：QueryBuilder 链式 O(N²) 拷贝消除（P8 + P9 + A1）

**问题**：`AddClause` 每次都 `new List<QueryClause>(_clauses)` + `new List<DbParameter>(_parameters)` ——链式构建 N 个子句产生 O(N²) 拷贝。`HasClause` 用 `Exists` 线性扫，每次 AddClause 都 O(n)。

**预期收益**：复杂查询（5+ Where/OrderBy）构建时间下降 50%+。

**改动方案**（三选一，推荐方案 3）：

1. **bitmask + lazy copy**（方案 1）：`byte _kindsMask` 替代 `HasClause` 线性扫；`AddClause` 用 `_copied` 标志位，首次写入时才 `new List`。
2. **class builder**（方案 2）：QueryBuilder 改 class，彻底消除 struct 副本隔离需求。
3. **immutable array + builder**（方案 3，推荐）：内部 `_clauses` 改 `ImmutableArray<QueryClause>`，`AddClause` 返回新实例时 COW 共享只读视图；`_parameters` 同理。

**风险**：高——QueryBuilder 是核心 API，struct 语义被用户依赖（虽然公共 API 表面不变）。必须：
- 先写 benchmark 对比三方案
- 跑全套 156 + 104 + 149 测试
- S3 反向验证：如改后查询性能反而下降，回退

---

### 🏅 优化 D：零分配 List + 参数批处理（P5 + P6 + P7）

**预期收益**：QueryAll 10K 行再省 ~3 次 Gen0 + 1 次 List 分配；BulkInsert 1K 行省 ~1万次 Add 虚调用。

**改动**：

| 文件 | 改动 |
|------|------|
| `QueryBuilderExtensions.cs:64` | `var list = new List<T>(16);` 起步 Capacity（或读 cache 命中时的 hint） |
| `QueryBuilderExtensions.cs:19,68` | 缓存命中时返回 `ImmutableArray<T>` 或 document 调用方复制语义 |
| `CommandFactoryEmitter.cs:251` | emit 改用 `cmd.Parameters.AddRange(preAllocatedArray)` —— 但 DbParameter 数组需先创建，权衡：批量 Add 数组 vs 逐个 Add，ADO.NET 实测批量快 2-3x |

**风险**：低——纯性能优化，API 不变。

---

### 🎖 优化 E：源生成器 emit 工程化（S2 + S3 + S4 + S5）

**预期收益**：编译速度提升 ~20%（大实体项目）；生成代码体积下降 ~15%。

**改动**：

| 文件 | 改动 |
|------|------|
| 所有 Emitter | `new StringBuilder()` → `StringBuilderPool.Rent()` + `using` 归还；`AddSource(hint, SourceText)` 替代 `AddSource(hint, string)` |
| RowFactoryEmitter + CommandFactoryEmitter | 合并为 `EntityFactory_{suffix}` 单文件（每实体省 ~12 行） |
| MigrationEmitter.cs:25 | 删 legacy `CreateTable` 字段（dialect 字典恒有 Sqlite 键，不可达） |
| CommandFactoryEmitter BindUpsert | 改为调用 BindInsert 后追加 PK 行 |

**风险**：低——emit 输出内容不变，只是工程化优化。快照基线需重生成。

---

### 🎁 优化 F：API 残留收口（API1-API7 + A7）

**预期收益**：代码清晰度 + 用户预期管理。

**改动**：

| API | 处理 |
|-----|------|
| `DiffAsync` | `[Obsolete("Use ValidateSchemaAsync")]` + 文档说明 |
| `[Column].Length/Precision/Scale/TypeName/StoreAs` | PALORM017 升级为 Error，强制用户明确"未实现" |
| `NamingConvention.ApplyNaming` | `[Obsolete("Compile-time only, see [Column]")]` |
| `WithMetrics(name)` | 删除 name 参数（当前不作为标签） |
| `GetRawConnection()` | 重命名 `DangerousGetRawConnection` + `[Obsolete("Use GetRawConnection")]` 渐进迁移 |
| `IRowFactory<T>` | 保持 public + XML doc（v3.1 已收口）；v4.0 评估后决定 |

**风险**：低——除 `GetRawConnection` 重命名外，都是 `[Obsolete]` 标注。

---

### 🚀 优化 G：AOT 最后 1 公里（T1 + T2 + T3）

**预期收益**：完整 AOT 兑现 + 文档完善。

**改动**：

| 文件 | 改动 |
|------|------|
| `DataSession.Query.cs:48,57,213` + `StoredProcBuilder.cs:98` | `Convert.ChangeType` 改为源生成特化（为每个 T 生成 switch case） |
| `test/PalORM.AotTest/Program.cs` | 增加枚举列 + Guid 列实体覆盖 |
| `docs/AOT部署指南.md` | 补充三方言原生 lib 的 RID 解析说明 |

**风险**：中——Convert.ChangeType 改源生成特化需新增 Emitter。

---

## 三、实施路线图

```
阶段 1（低风险快速收益，~2 天）
  ├─ 优化 A：CommandFactory Converter 单例（P4 + S1） ← 最高收益
  ├─ 优化 D：List Capacity + 参数批处理（P5+P6+P7）
  └─ 优化 F：API [Obsolete] 收口（API1-API7）
  → 快照重生成 + 全套测试 + 基准对比

阶段 2（中等风险核心收益，~3 天）
  ├─ 优化 B：CRUD 路径 Volatile.Read 合并 + 预构建 SQL（P1+P2+P3+A3）
  └─ 优化 E：源生成器 emit 工程化（S2+S3+S4+S5）
  → 快照重生成 + 全套测试 + 基准对比

阶段 3（高风险高收益，~5 天）
  ├─ 优化 C：QueryBuilder 链式 O(N²) 拷贝消除（P8+P9+A1）
  │  └─ 先 benchmark 三方案 → 选最优 → 全套测试 → S3 反向验证
  └─ 优化 G：AOT 最后 1 公里（T1+T2+T3）
  → 架构评审 + AOT 实际发布验证

阶段 4（收尾，~1 天）
  ├─ 文档全面更新（README + API参考 + 架构设计）
  ├─ CHANGELOG 登记 v4.0
  ├─ lessons.md 登记新教训
  └─ 版本号升级 3.1.0 → 4.0.0（Breaking Change：GetRawConnection 重命名）
```

---

## 四、预期优化后基准

| 操作 | v3.1 实测 | v4.0 预期 | vs ADO.NET | 主要贡献 |
|------|:---:|:---:|:---:|------|
| QueryAll 10K | 4.73ms (123%) | **~4.2ms (110%)** | 3.83ms (100%) | 优化 D（List Capacity）+ 优化 C（QueryBuilder） |
| GetByKey | 31μs (151%) | **~24μs (117%)** | 20.56μs (100%) | 优化 B（CRUD Volatile 合并 + 预构建 SQL） |
| Insert | 32.65μs (137%) | **~28μs (117%)** | 23.91μs (100%) | 优化 A（Converter 单例）+ 优化 D（参数批处理） |
| Update | 23.15μs (53%) | ~22μs | 33.91μs (100%) | 优化 A（Converter 单例） |
| Delete Physical | 37.24μs (140%) | **~33μs (124%)** | 26.59μs (100%) | 优化 A（Converter 单例） |
| BulkInsert 1K（含 Converter） | — | **-50% Gen0 分配** | — | 优化 A 落地（最大收益场景） |

---

## 五、验证标准

```
1. dotnet build -c Debug --no-incremental → 0 警告 0 错误
2. PALORM_UPDATE_SNAPSHOTS=1 dotnet run --project test/PalORM.SourceGen.Tests → 快照全部重生成
3. dotnet test test/PalORM.Core.Tests → 156/156 全绿
4. dotnet test test/PalORM.SourceGen.Tests → 104/104 全绿
5. dotnet test test/PalORM.Integration.Tests → SQLite 149/149 全绿
6. bash scripts/tech-debt-scan.sh → 12/12 全通过
7. dotnet publish test/PalORM.AotTest -p:PublishAot=true → 0 警告 + 运行 PASSED
8. dotnet run --project bench/PalORM.Benchmarks -c Release → 基准对比验证
9. 架构评审通过（优化 C/G 是 Breaking Change）
```

---

## 六、风险评估与回退

| 优化 | 风险等级 | 回退策略 |
|------|:---:|----------|
| A（Converter 单例） | 低 | emit 局部变更，git revert 单提交 |
| B（CRUD Volatile 合并） | 中 | 新增字段不影响旧调用，可渐进 |
| C（QueryBuilder 重构） | **高** | 必须保留 struct QueryBuilder 分支，benchmark 不达预期回退 |
| D（List + 参数批处理） | 低 | 纯 micro-opt，不影响 API |
| E（emit 工程化） | 低 | 输出等价，快照验证 |
| F（API 收口） | 低 | [Obsolete] 可后期移除 |
| G（AOT 收口） | 中 | Convert.ChangeType 源生成特化需新 Emitter |

---

## 七、与 v3.1 的关系

| 维度 | v3.1（已完成） | v4.0（本方案） |
|------|---------------|---------------|
| 核心目标 | IRowFactory 虚分发消除 | QueryBuilder O(N²) 消除 + emit 不对称收口 |
| 主要优化点 | 3 个（Func 委托 + Converter 单例 + 快照合并） | 7 个（A-G） |
| 预期收益 | QueryAll 177%→123% | QueryAll 123%→110% |
| 风险 | 低 | 中-高（优化 C 是核心 API 重构） |
| 实施周期 | 1 轮会话 | 3-4 轮会话（分阶段） |

**关键差异**：v3.1 是"消除明显错误"（虚分发、重复分配），v4.0 是"逼近理论极限"（struct 副本语义重构、emit 工程化、AOT 全链路）。

---

## 八、调研产出引用

本方案基于 4 路并行深度调研：

| 调研维度 | 关键发现数 | 引用 agent |
|---------|:---:|-----------|
| 核心架构（DataSession/QueryBuilder/IDbProvider） | 12 个改进点（O1-O12） | `agent_e17c4e5b` |
| 源生成器（Emitter/Analyzer/Validation） | 6 个 emit 优化 + 诊断规则清理 | `agent_d6c96fc5` |
| 性能 hot path + AOT | 12 个零分配机会 + AOT 检查清单 | `agent_1e63d6e4` |
| API 设计 + 三 Provider 差异 | 7 个 API 收口 + 能力不对等分析 | `agent_93ccc351` |

详细调研报告见本会话上下文（4 个 Explore Agent 产出）。
