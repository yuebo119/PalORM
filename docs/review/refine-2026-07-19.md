# PalORM 精炼报告 — 2026-07-19

> 基线：a04c8d7（评审收口后）
> 流程：refine-scan.sh 阶段 0 → P0→P1 逐项执行 → 每项 build · 每批全量测试 + AOT
> 约束：零行为变更 · 零 public API 签名变更 · AOT 安全（零反射引入）

## 执行清单

### P0（🟢 零风险）

| 操作 | 内容 | 涉及文件 | 量化效果 | 验证 |
|:--:|------|---------|---------|:--:|
| M1 | 裸 `new List<T>()` → 集合表达式 `[]`（18 处；带容量/拷贝构造的 6 处不动） | Core 6 文件 + SourceGen 2 文件 | 语法现代化；`[]` 走 CollectionBuilder 零开销 | ✅ |
| M6 | `?? throw new ArgumentNullException` → `ArgumentNullException.ThrowIfNull`（2 处） | DataSession.cs 构造函数 | -2 行；throw 帮助方法可内联 | ✅ |

### P1（🟡 需评估）

| 操作 | 内容 | 涉及文件 | 量化效果 | 验证 |
|:--:|------|---------|---------|:--:|
| M9 | `readonly object` 锁字段 → `System.Threading.Lock`（4 类：SessionOperationState/GridReader/Resilience/PalORM_Runtime；PgNotificationListener 已是 Lock） | Core 4 文件 | lock 语句走 `Lock.EnterScope()` 专用路径——无对象头锁膨胀、无 Monitor 装箱面 | ✅ 并发测试全绿 |
| O9 | 查询执行热路径 `_dialect.ToString()` → `GetName()` switch 常量（4 处：ToListAsync/QueryMultiple/ExecuteNonQuery/ExecuteScalarObserved） | SqlDialect.cs（新增内部扩展）+ QueryBuilderExtensions.cs | 每次查询观测省 1 次字符串分配（Enum.ToString 每调用新串；switch 返回 interned 常量） | ✅ |
| O5 | BuildSql 热路径 LINQ `Where` → 手写循环（4 处：AppendComments/AppendCtes/Window 投影/AppendClauses） | QueryBuilder.cs | 每次 BuildSql 省 4 个委托 + 4 个迭代器分配 + AppendCtes 的 ToArray 中间数组 | ✅ |

### 评估后跳过（附依据）

| 操作 | 候选 | 跳过原因 |
|:--:|------|---------|
| M10 | `params string[]`（IndexAttribute/PgNotificationListener/TestDb.FromRows） | 全部是 public API 签名——精炼红线"不改 public API"；Attribute 构造参数不支持 Span |
| O1 | PalORM_Runtime.Register 内 6 处 `new Dictionary` | 发布前中间态（构造完即 ToFrozenDictionary），基线表已标注不动 |
| O8 | BulkDeleteAsync(object) 等 9 处 object 参数 | public API 泛型化属重构；基线表已裁决跳过 |
| A6 | 13 处 SuppressMessage | 逐项核对：全部有 Justification 且对应场景仍在（清理路径 CA1031/所有权转移 CA2000 等） |
| A8 | 10 处回退兼容路径候选 | CommandSqls legacy 回退已在 GetCommandSqls 改为明确失败（前轮 SEC-01）；其余为 Provider 方言回退属设计而非兼容残留 |
| M8 | 17 处 struct 声明 | QueryBuilder 是写时复制可变 struct（不可 readonly）；record struct 语义变更风险 🔴，本轮不动 |
| M2/M3 | 主构造函数/required 迁移 | 🔴 风险 + DataSession 构造含校验逻辑，收益（-4行）不抵回归验证成本 |

## 精炼前后对比

- 代码变化：11 文件，+52/-48 行（新增 SqlDialectExtensions 15 行换热路径 4 处零分配）
- 热路径收益：每次 `ToListAsync` 执行减少 ~5 个瞬时分配（1 字符串 + 4 委托/迭代器）；BuildSql 减少 1 个中间数组
- 锁基础设施：4 个核心并发类迁移到 .NET 9+ `Lock` 类型

## 验证记录

```
build 0W0E · Core.Tests 148/148 · Integration.Tests 154/154（真库）·
SourceGen.Tests 87/87 · SQLite AOT publish + 原生运行 PASSED
```

TableModel.cs 首次 python 全文写入引入 CRLF→LF 噪音（±454 行假 diff），已还原后二进制模式精确替换（实际 ±6 行）——精炼工具纪律：批量文本操作前核对行尾。
