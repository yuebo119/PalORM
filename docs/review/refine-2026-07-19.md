# PalORM 精炼报告 — 2026-07-19

> 基线：a04c8d7（评审收口后）
> 流程：refine-scan.sh 阶段 0 → P0→P1 逐项执行 → 每项 build · 每批全量测试 + AOT
> 约束：零行为变更 · 零 public API 签名变更 · AOT 安全（零反射引入）

## 执行清单

### P0（🟢 零风险）

| 操作 | 内容 | 涉及文件 | 量化效果 | 验证 |
|:--:|------|---------|---------|:--:|
| M1 | 裸 `new List<T>()` → 集合表达式 `[]`（18 处；带容量/拷贝构造的 6 处不动） | Core 6 文件 + SourceGen 2 文件 | 语法现代化；`[]` 走 CollectionBuilder 零开销 | ✅ |
| M6 | `?? throw new ArgumentNullException` → `ArgumentNullException.ThrowIfNull`（2 处） | DataSession.cs 构造函数 | -2 行；throw 帮助方法可内联 | ✅ |

### P1（🟡 需评估）

| 操作 | 内容 | 涉及文件 | 量化效果 | 验证 |
|:--:|------|---------|---------|:--:|
| M9 | `readonly object` 锁字段 → `System.Threading.Lock`（4 类：SessionOperationState/GridReader/Resilience/PalORM_Runtime；PgNotificationListener 已是 Lock） | Core 4 文件 | lock 语句走 `Lock.EnterScope()` 专用路径——无对象头锁膨胀、无 Monitor 装箱面 | ✅ 并发测试全绿 |
| O9 | 查询执行热路径 `_dialect.ToString()` → `GetName()` switch 常量（4 处：ToListAsync/QueryMultiple/ExecuteNonQuery/ExecuteScalarObserved） | SqlDialect.cs（新增内部扩展）+ QueryBuilderExtensions.cs | 每次查询观测省 1 次字符串分配（Enum.ToString 每调用新串；switch 返回 interned 常量） | ✅ |
| O5 | BuildSql 热路径 LINQ `Where` → 手写循环（4 处：AppendComments/AppendCtes/Window 投影/AppendClauses） | QueryBuilder.cs | 每次 BuildSql 省 4 个委托 + 4 个迭代器分配 + AppendCtes 的 ToArray 中间数组 | ✅ |

### 评估后跳过（附依据）

| 操作 | 候选 | 跳过原因 |
|:--:|------|---------|
| M10 | `params string[]`（IndexAttribute/PgNotificationListener/TestDb.FromRows） | 全部是 public API 签名——精炼红线"不改 public API"；Attribute 构造参数不支持 Span |
| O1 | PalORM_Runtime.Register 内 6 处 `new Dictionary` | 发布前中间态（构造完即 ToFrozenDictionary），基线表已标注不动 |
| O8 | BulkDeleteAsync(object) 等 9 处 object 参数 | public API 泛型化属重构；基线表已裁决跳过 |
| A6 | 13 处 SuppressMessage | 逐项核对：全部有 Justification 且对应场景仍在（清理路径 CA1031/所有权转移 CA2000 等） |
| A8 | 10 处回退兼容路径候选 | CommandSqls legacy 回退已在 GetCommandSqls 改为明确失败（前轮 SEC-01）；其余为 Provider 方言回退属设计而非兼容残留 |
| M8 | 17 处 struct 声明 | QueryBuilder 是写时复制可变 struct（不可 readonly）；record struct 语义变更风险 🔴，本轮不动 |
| M2/M3 | 主构造函数/required 迁移 | 🔴 风险 + DataSession 构造含校验逻辑，收益（-4行）不抵回归验证成本 |

## 精炼前后对比

- 代码变化：11 文件，+52/-48 行（新增 SqlDialectExtensions 15 行换热路径 4 处零分配）
- 热路径收益：每次 `ToListAsync` 执行减少 ~5 个瞬时分配（1 字符串 + 4 委托/迭代器）；BuildSql 减少 1 个中间数组
- 锁基础设施：4 个核心并发类迁移到 .NET 9+ `Lock` 类型

## 验证记录

```
build 0W0E · Core.Tests 148/148 · Integration.Tests 154/154（真库）·
SourceGen.Tests 87/87 · SQLite AOT publish + 原生运行 PASSED
```

TableModel.cs 首次 python 全文写入引入 CRLF→LF 噪音（±454 行假 diff），已还原后二进制模式精确替换（实际 ±6 行）——精炼工具纪律：批量文本操作前核对行尾。

---

# 第二轮 — [人工] 标注项审查与执行（同日）

> 范围：阶段 0 扫描中标注 [人工] 的 7 项（A1/A2/A3/A7/M2/M7/O2/O7）+ 首轮遗留 M4/O6/O3/O4/M8 评估。
> 原则：可执行的执行；不可执行的产出有据结论，写入本报告作为下轮基线。

## 已执行

| 操作 | 内容 | 量化效果 | 验证 |
|:--:|------|---------|:--:|
| A2 | 事务清理骨架去重：DataSession/QueryBuilderExtensions/MultiValueBulkInsert 三份逐字复制的 RollbackPreservingAsync/DisposeTransactionPreservingAsync 收敛到新 `TransactionCleanup` 静态类（PG Provider 侧保留——跨程序集刻意独立，已有 ITM-412 防漂移锚点） | **-44 行**（3×2 份私有实现 → 1 份共享 +32 行新文件） | ✅ |
| O2 | WhereIn/WhereNotIn 的 batches/parameters 按 items.Count 预分配；GetParametersForKinds 按 _parameters.Count 预分配 | 大 IN 列表（>500 项）零扩容拷贝 | ✅ |

## 评估后不执行（附据）

| 操作 | 候选 | 结论依据 |
|:--:|------|---------|
| A1 | 引用计数扫描 10 个低引用候选 | 逐个 Read 证伪：RegistryEmitter/PalORM_RegistryInitializer 是生成物类名（字符串引用，grep 不可见）；PostgreSqlExtensions.WhereJson 有 4 个测试消费；SensitiveDataAttribute 是文档承诺的公共注解（API 参考 TM3 + 踩坑 77/89/278）；NpgsqlNotificationConnection 经工厂 lambda 引用；SqlDialectExtensions.GetName 4 个调用点。**真零引用：0 个** |
| A3 | ≤10 行文件仅 Polyfills.cs(8 行)/IRowFactory.cs(11 行) | Polyfills 是 netstandard2.0 编译要求；IRowFactory 是公共契约接口——独立文件是设计不是冗余 |
| A7 | 内建拦截器类 | TracingInterceptor/MetricsInterceptor 在历史精炼中已删；IQueryInterceptor.cs 现仅含接口+3 个 record struct，无未使用内建类 |
| M2 | GridReader/StoredProcBuilder/ResilienceExecutor 构造 | 构造体内有参数处理逻辑（排序/默认值/双构造重载），主构造函数化收益 -3 行但打破 readonly 字段+逻辑分布，可读性负收益 |
| M4 | is 类型链 1 处（PalORMAnalyzer:264） | 该 else-if 链各分支报不同诊断+提前 return 形态，switch 表达式无法表达副作用分支——保持现状 |
| M8 | 4 个非 record struct | QueryBuilder（写时复制可变 struct，readonly 化即破坏设计）；SessionOperationLease/CrudMetadata/EquatableArray 均已 readonly 且自定义相等/释放语义，record 合成 Equals 反而引入行为差异风险 |
| M7/O4 | yield 3 处 | DataSession.QueryAsyncEnumerable 是公共 IAsyncEnumerable API（签名红线）；PalORMAnalyzer 2 处是编译期路径非热路径 |
| O3 | Core 13 处 ToArray/ToList 逐点 | 写路径（SaveCoreAsync/BuildMySqlUpsertSql 的 updateColumns.ToArray）每实体类型执行一次 SQL 构建、结果送 string.Join——Span 化需重写 Join 逻辑，收益不成比例；构造期/注册期 8 处非热路径；WhereIn 的 `as IReadOnlyList ?? ToList` 已是防重复枚举的正确形态 |
| O6 | 2 处 `+=` | 均为单次条件追加（非循环累加），无碎片问题——扫描误报形态 |
| O7 | ValueTask 数据流 | 全库 ValueTask 均为直接 await 单次消费（无变量暂存/无多点 await）；ExecuteWithResilience 内层是 Task 非 ValueTask。零风险点 |

## 第二轮验证

```
build 0W0E · Core 148/148 · Integration 154/154（真库）· SourceGen 87/87 ·
SQLite AOT publish + 原生运行 PASSED · gate 25/25
```

## 基线表刷新（供下轮）

[人工] 7 项全部出清（2 执行 + 5 有据关闭）；机械扫描剩余大项 O3(47→冷路径为主)/M9(32 处 lock 语句本体——Lock 类型已迁,语句不必改)/O9(21 处非 Enum 场景) 均已定性,下轮扫描命中数变化 ±30% 内无需重评。
