# 复制到仓库根目录为 .env.test（已被 .gitignore 排除），填入真实值。
# 禁止将真实凭据提交到版本控制。
PALORM_PG_CONNECTION='Host=<host>;Username=<user>;Password=<password>;Database=<db>'
PALORM_MYSQL_CONNECTION='Server=<host>;User=<user>;Password=<password>;Database=<db>'
