# PalORM 全量审计报告

> 审计日期：2026-07-17
> 基线提交：`c80f983`
> 审计框架：`.ai/audit-system` v1.1（本轮为系统修复后的验证性审计）
> 审计范围：git 跟踪的 `src/**/*.cs` 40 个手写文件，7,221 行；csproj、CI、scripts、docs 作为证据源
> 排除：`*.g.cs` 生成文件（仅用于验证生成语义）；test/bench/tools 作为交叉验证证据
> 方法：6 个独立审计流并行执行，每流强制加载误判知识库，全部发现基于完整方法体读取（非 grep 计数）
>
> ⚠️ **事后勘误（2026-07-17 晚）**：本报告写成后发生 Git 仓库重建（f0df771），两项结论随之失效：
> 1. §1"凭据历史已重写清除"——重建历史重新包含含真实凭据的旧版 `set-test-env.sh`，AUD-001 重新开放（见整改账本追加）；
> 2. §11 引用的 `verify-ai-system.sh`——文件在重建中丢失，已按 7 项检查口径重建（误判知识库新增模式 P8 记录此教训）。

## 1. 结论

**综合评分 86.5 / 100（上轮 40.2）。** 上轮（2026-07-11）确认的 1 项 P0 与 15 项 P1 中，除 QUERY-001（已知、账本在案）外全部验证为已修复。本轮新发现 0 项 P0、1 项 P1（文档 API 签名不存在）、6 项 P2/P3 级低危观察。

上轮四大最高优先级事项的现状：
1. **凭据暴露（P0）** → 仓库侧已整改：脚本从 gitignore 的 `.env.test` 加载且不回显值；历史已重写清除；G9 转绿。剩余数据库侧轮换（人工）。
2. **SaveAsync / ConcurrencyCheck / 读写分离 / SoftDelete 语义缺失** → 全部验证有真实实现（UPSERT 分方言落地、乐观锁 SQL 与绑定参数逐位对齐、ForRead 真实路由 + 租约释放、软删过滤注入全查询路径）。
3. **OwnedJson 反射型 STJ** → 已改为强制 `JsonSerializerContext` + `JsonTypeInfo<T>` 源生成重载，生成产物实测确认。
4. **公共字符串进入 SQL 结构** → LISTEN channel / Tag 注释 / SAVEPOINT 名全部经 QuoteIdentifier 或校验，三 Provider 引用实现（双写转义）逐一验证正确。

## 2. 快照

| 指标 | 本轮结果 | 证据 |
|---|---:|---|
| 手写生产源码 | 40 文件，7,221 行 | `git ls-files` |
| 最大文件 | `DataSession.cs` 1,080 行 | `wc -l` |
| Release 构建 | 0 警告，0 错误 | `review-snapshot.sh`（no-incremental） |
| Core 单测 | 106/106 | `dotnet test` |
| SourceGen 单测 | 51/51 | `dotnet test` |
| 集成测试 | 119/123（4 项 PG/MySQL 外部服务不可达） | `dotnet test` |
| SQLite Native AOT | publish 0 警告 + 原生运行 PASS | 本机实测 `PalORM AOT verification PASSED` |
| 门禁 | 23/23 PASS（含 G9） | `gate-check.sh` exit 0 |
| 空壳扫描 | PASS | `stub-check.sh` exit 0 |
| Git 状态 | 干净 | `git status` |

### 2.1 验证限制（区分"没找到"和"不存在"）

- PG/MySQL 本机不可达：4 项集成测试、两个 AOT 原生运行未在本机执行（CI 已配置服务容器 job，但本轮无远端执行证据）。
- 302 坑防御为抽样核对（经六流间接覆盖），未逐坑验证。
- 未执行性能 profiling 与网络故障注入。

## 3. 层 1：六流审计（53.5/60，权重 30%）

| 流 | 分数 | 发现 | 摘要 |
|---|:---:|---|---|
| 架构流 | 7/10 | ARCH-01~04 | 依赖方向/命名空间全净；上轮"配置无实现"三项已落地；ARCH-04（高）QUERY-001 仍在 |
| 安全流 | 8.5/10 | SEC-01 | 上轮三处结构注入全部修复；凭据卫生良好；SEC-01 为死代码上的引用缺口 |
| 资源流 | 9.5/10 | 零发现 | CreateAsync 失败释放已修复；租约/GridReader/三 Provider 批量清理全净 |
| 并发流 | 9.5/10 | 零缺陷 +1 观察 | 乐观锁参数逐位对齐；熔断器状态持久 + generation 防陈旧；CacheStore 过期条目不清理（容量观察） |
| 错误流 | 9.5/10 | ERR-01/02（⚠低） | 52 个 catch 逐个验证；取消传播全净；主异常保留机制统一正确 |
| AOT 流 | 9.5/10 | AOT-02（⚠） | 运行时零反射面、零 IL 抑制、零 DAM/RUC；账本 AOT-001 状态落后于代码 |

## 4. 发现清单

### P1（本迭代内修）

| ID | 位置 | 描述 | 危害 | 可信度 |
|---|---|---|---|:---:|
| ARCH-04 | `QueryBuilder.cs:24-25,561-567` | struct 复制后共享 `_clauses` List，显式保存 builder 再分支时条件互相污染，静默返回错误数据。已知项 QUERY-001（账本"待实施"），本轮确认风险仍在 | 高 | ✅ |
| ARCH-02 | `docs/API参考.md:99` | W3 声明 `UpdateColumnsAsync<T>(id,partial)` 在源码中不存在（实际为 `UpdateColumns<T>()` 无参返回 QueryBuilder）。用户按文档调用编译失败 | 中 | ✅ |

### P2/P3（计划/评估）

| ID | 位置 | 描述 | 危害 |
|---|---|---|---|
| SEC-01 | `MigrationEmitter.cs:82,85` | legacy `BuildCreateTable` 重载标识符未经 QuoteIdentifier；运行时无消费者（死代码），建议删除 | 低 ⚠ |
| CONC-观察 | `CacheStore.cs:13` | 过期条目仅判定不移除，key 空间发散时无界增长 | 低 |
| ERR-01 | `FormattableSqlFormatter.cs:48` | 泛型 FormatException 且消息缺实际索引值 | 低 ⚠ |
| ERR-02 | `PgNotificationListener.cs:134-140` | OnError 无订阅者时后台监听终止无痕（文档化设计权衡，建议 ILogger 兜底） | 低 ⚠ |
| ARCH-01 | `DataSession.cs:6-7` | Core 依赖 `Microsoft.Extensions.Logging`（共享框架、零第三方传递）超出"仅 BCL+ADO.NET"声明口径——对应账本 DOC-001 | 低 ✅ |
| ARCH-03 | `docs/API参考.md` 多处 | 实现位置行号系统性漂移（抽查 5 项中 4 项偏移 6~208 行） | 低 ✅ |
| AOT-02 | 账本 `implementation-tasks-2026-07-15.md:44` | AOT-001 标"待实施"但 AotTest.Pg/MySql/PackageConsumer + CI 服务容器 job 均已存在——账本低报已交付能力 | 低 ⚠ |

## 5. 层 2：十维度（85/100，权重 40%）

构建卫生 10（0 警告 warnings-as-errors）· 测试 9（274/278，4 项环境性失败）· 门禁 10（23/23）· 资源管理 10 · 并发 9 · 错误处理 9 · 安全 9 · AOT 9 · 文档一致性 5（W3 不存在 + 行号漂移 + 账本滞后，层 1 发现封顶降分）· API 设计 5（QUERY-001 分支语义陷阱未修）。

## 6. 层 3：PalORM 专项（60/70 → 85.7/100，权重 30%）

| # | 检查项 | 分 | 依据 |
|---|---|:---:|---|
| 1 | Provider 插件依赖方向 | 10 | 三 Provider 零跨引用、SourceGen 零运行时依赖（csproj + using 全量核对） |
| 2 | API 完整性 | 7 | 112 实现口径成立，但 W3 文档签名不存在 + 行号漂移 |
| 3 | AOT 兼容 | 10 | 零反射面、零 IL 抑制、STJ 反射开关四项目关死、SQLite 原生运行实测 |
| 4 | 源生成器正确性 | 9 | 51/51 测试；SEC-01 死代码缺口 |
| 5 | FormattableString 参数化 | 10 | 插值→DbParameter 全路径验证，零值拼接 |
| 6 | 302 坑防御 | 8 | ⚠ 抽样（经六流间接覆盖），未逐坑核对 |
| 7 | 文档-代码-注释三方一致 | 6 | DOC-001 待实施、AOT-02 账本滞后、ARCH-03 行号漂移 |

## 7. 综合评分

```
综合 = 层1 89.2×30% + 层2 85×40% + 层3 85.7×30% = 86.5 / 100
```

依据 P2 原则（零 bug ≠ 满分），层 2/层 3 因文档一致性失真与已知未修项封顶。

## 8. 趋势

| 指标 | 2026-07-11 | 2026-07-17 | 变化 |
|---|:---:|:---:|---|
| 综合评分 | 40.2 | 86.5 | 上轮 16 项 P0/P1 中 15 项验证已修复 |
| P0 | 1（凭据） | 0（仓库侧闭环，余数据库轮换） | 历史重写 + G9 转绿 |
| P1 | 15 | 2（1 项为已知在案的 QUERY-001） | 核心语义/注入/资源/并发/AOT 全部落地 |
| 构建警告 | 1,289 | 0 | warnings-as-errors 生效 |
| 测试 | 38 单元 + 74/78 集成 | 274/278 | 大幅回填行为测试 |
| 门禁 | 失败（脚本缺陷+G9） | 23/23 PASS | 脚本修复 + 凭据整改 |

评分跃升的主因是真实修复而非口径放松：本轮每项"已修复"结论均由独立审计流在 c80f983 源码上重读完整方法体确认，未采信账本自述。

## 9. 风险预判（四维加权）

| 因子 | 权重 | 分值 | 依据 |
|---|:---:|:---:|---|
| 变更频率 | 30% | 20 | 近 6 提交以治理/安全/文档为主，代码变动低 |
| 缺陷密度 | 30% | 35 | 1 高危已知（QUERY-001）+ 6 低危 |
| 覆盖盲区 | 25% | 50 | PG/MySQL 运行时无本机证据、302 坑未逐坑、无性能基线 |
| 复杂度 | 15% | 60 | DataSession 1,080 行、QueryBuilder 659 行 |

```
风险 = 20×30% + 35×30% + 50×25% + 60×15% = 38/100（中低）
```

最可能的近期故障：
1. 用户显式保存 QueryBuilder 中间变量做多分支查询 → 条件污染静默返回错行（QUERY-001）。
2. 用户按 API 参考 W3 签名调用 `UpdateColumnsAsync` → 编译失败（文档失真）。
3. 长时间运行 + 发散 cache key → CacheStore 内存缓慢增长。

## 10. 建议修复顺序

1. QUERY-001（ARCH-04）：`AddClause` 改写时复制（copy-on-write）或分支时强制 clone——高危静默数据错误，P1。
2. ARCH-02：修正 API参考.md W3 条目为真实签名；顺带批量刷新行号（ARCH-03）或改用"文件名+方法名"替代行号。
3. 账本对账：AOT-001 改"已完成待 CI 绿"（AOT-02）；DOC-001 补 Core 依赖真实口径说明。
4. SEC-01：删除 MigrationEmitter legacy 重载与 CreateTable 常量发射。
5. CacheStore 过期清理 + 容量上限；ERR-01 消息补索引值；ERR-02 加 ILogger 兜底（低优先）。

## 11. 方法论自证（本轮为 AI 系统验证性审计）

- 误判知识库 8+6 条模式全部前置加载，六流报告中均有显式规避记录（P2 Provider 独立、P4/P6 ModuleInitializer、模式 1 完整 lock 块、模式 3 前置 OCE、模式 7 grep→Read）。
- 上轮教训（"表面 grep + 弱断言"导致 96.4 虚高）本轮对策：所有"已修复"均重读方法体，账本声明（GRID-001/LIFE-001/TX-001）逐项独立复验并全部属实。
- 提示词修正后的引用（编码规范.md/踩坑目录.md/API参考.md 113 口径）在本轮实际使用中全部可解析，`verify-ai-system.sh` 7/7 PASS。
