# PalORM 全面评审报告：90c2039

> 评审日期：2026-07-17
> 评审范围：src/ 全部手写源码（40 文件，7,231 行）+ 构建/门禁/测试/文档交叉验证
> 评审方法：七流并行逐行审计（架构/安全/资源/并发/错误/AOT/生成语义）
> 评审基线：`90c203974eb04a7807d78fce9751cb3605167c4d`（dev）
> 结论：**REQUEST_CHANGES** —— 2 项 P0（QUERY-001 残留 + 缓存共享可变列表）、6 项 P1

---

## 段 1：范围与方法

**评审范围**：
- `src/PalORM.Core`（21 文件）、`src/PalORM.SourceGen`（13 文件）、三 Provider、`src/PalORM.Testing`
- 生成物语义验证：`test/PalORM.AotTest/obj/generated/**/*.g.cs`（仅验证生成语义，不做风格评审）
- 交叉验证证据源：`docs/架构设计.md`、`docs/API参考.md`、`scripts/`、`.github/workflows/ci.yml`、`docs/review/implementation-tasks-2026-07-15.md`

**明确不评审**：
- `*.g.cs` 生成文件的代码风格（自动排除，仅验证语义）
- `test/`、`bench/`、`tools/PalORM.Scaffold` 手写代码（仅作交叉验证证据）
- 性能 profiling、网络故障注入、真实 PG/MySQL 数据库运行行为（本机无凭据）
- 第三方驱动（Npgsql/MySqlConnector/Microsoft.Data.Sqlite）内部行为

**抽样策略**：src/ 全量逐行（七流各自逐文件读取完整方法体，非 grep 计数）；设计决策抽查 9/18 项；特性组合实体抽 3 组；PALORM 诊断测试覆盖全量核对。

**盲区声明**：
- PG/MySQL 集成测试与 AOT 原生运行未在本机执行（凭据文件 `.env.test` 不存在，4 项集成测试环境性失败）
- 本轮未重新执行 `dotnet publish /p:PublishAot=true`（AOT 结论基于静态审计 + 历史实测记录）
- 302 坑防御未逐坑核对（经七流间接覆盖）
- 复合主键路径、`SqlTemplateEmitter` 畸形输入、驱动版本级 Dispose 语义未深查
- 并发发现（CONC-01 场景 B/C、RES-04）为静态推导，未编写复现测试动态确认

---

## 段 2：评审基线

```
═══════ 评审基线快照 ═══════
时间：2026-07-17 19:50:22 CST
分支：dev
提交：90c203974eb04a7807d78fce9751cb3605167c4d
短提交：90c2039
SDK：11.0.100-preview.6.26359.118
工作树：有未提交变更

─── 最近一次提交变更 ───
 .ai/README.md                                      | 57 ++++++++++++++++
 .ai/audit-system/perspective-stats.md              | 39 +++++++++++
 .ai/audit-system/prompt.md                         | 22 ++++--
 .ai/gate-system/prompt.md                          | 12 ++--
 .ai/refine-system/prompt.md                        | 17 +++++
 .ai/review-system-v2.md                            | 17 ++++-
 docs/review/implementation-tasks-2026-07-15.md     |  1 +
 docs/编码规范.md                                    |  4 +-
 scripts/gate-check.sh                              | 38 +++++++++++
 scripts/verify-ai-system.sh                        | 19 ++++++
 src/PalORM.Testing/TestDb.cs                       | 78 +++++++++++-----------
 11 files changed, 250 insertions(+), 54 deletions(-)

─── 文件统计 ───
C# 源文件：40 · 测试文件：26 · 文档文件：8

─── 构建状态 ───
    0 Warning(s)  0 Error(s)  构建退出码：0
```

**工作树未提交变更说明**：`git diff --stat` 显示 59 文件 9,622 插入 = 9,622 删除，`git diff --ignore-cr-at-eol` 为空——全部为行尾符（CRLF/LF）差异，零语义变更，不影响评审有效性。

**补充验证**（本轮实测）：
- `gate-check.sh`：25/25 PASS（含新增 G24/G25）
- `verify-ai-system.sh`：9/9 PASS · `stub-check.sh`：PASS · `doc-consistency-check.sh`：8/8 PASS
- Core 测试 111/111 · SourceGen 51/51 · Integration 119/123（4 项失败均为 PG/MySQL 凭据缺失：`No password has been provided` / `Access denied`，环境性而非代码缺陷）

---

## 段 3：准确性分析

> 七流独立审计后由主评审对全部 P0/P1 逐项二次 Read 源码交叉验证。同一缺陷多流命中时合并为单条（归属唯一）。

### P0/P1 发现（全部 ✅ 双源验证）

| ID | 发现 | 位置 | 可信度 | 信息源 |
|:--:|------|------|:--:|--------|
| ITM-101 | QUERY-001 仅部分修复：`EnsureWritable` 的 `_writable` 标志随 struct 复制被拷贝，已写入的 builder 复制后仍共享 `_clauses`/`_parameters`；且 `[SoftDelete]`/`[TenantAware]` 实体经 `From<T>()` 内部 `AddDefaultFilter`/`Where` 出生即 `_writable=true`，原始缺陷对这两类实体完全未修——分支条件互相污染，静默返回错误数据。账本已将 QUERY-001 标"已完成"，账实不符。配套测试 4 用例只覆盖"首次写入前复制"场景 | `src/PalORM.Core/QueryBuilder.cs:571-577`、`src/PalORM.Core/DataSession.cs:128-131` | ✅ | 并发流 Read + 主评审复读 + 测试用例核对 |
| ITM-102 | 查询缓存存取同一 `List<T>` 实例：`ToListAsync` 命中直接 `return cached`，`Set` 存入即将返回的列表。多调用方共享可变列表——任一方修改污染缓存与所有读者；并发枚举+修改抛异常或读撕裂数据 | `src/PalORM.Core/QueryBuilderExtensions.cs:14-15,52`、`src/PalORM.Core/CacheStore.cs` | ✅ | 并发流 + 资源流独立命中 + 主评审复读 |
| ITM-103 | `WhereJson` 功能性损坏：双层 FormattableString 嵌套使外层格式串为 `{0}`、内层 FormattableString 对象整体成为参数值——生成 `WHERE @p0` 且绑定不可序列化对象，运行时必然失败；文档声称 `column->>'path'` 查询，实现无 `->>`。文档 PG1 项公开宣传该 API。修复时 `jsonPath` 必须经 QuoteIdentifier，否则打开标识符注入面 | `src/PalORM.PostgreSql/PostgreSqlExtensions.cs:9`、`docs/API参考.md:201` | ✅ | 架构流 + 安全流独立命中 + 主评审复读 |
| ITM-104 | `[Index]`/`[Unique]`/`[ForeignKey]`/`[DefaultValue]`/`[Column(Length=…)]` 静默失效：MigrationEmitter 生成的 `CreateIndex_*`/`FK_*` 常量全库零消费者（`MigrateAsync` 只执行 `CreateTableSql`）；TableModel 对 Length/Precision/Scale/StoreAs 传 null 占位、indexes 恒空。用户标注得到零效果且无诊断。叠加缺陷：FK DDL 用 `ALTER TABLE ADD CONSTRAINT`（SQLite 不支持）、FK 列名取属性名而非映射列名。文档 M4/BA11 宣传这些注解 | `src/PalORM.SourceGen/MigrationEmitter.cs:30-58`、`src/PalORM.SourceGen/TableModel.cs:86,98,117`、`src/PalORM.Core/DataSession.cs:523-537` | ✅ | 架构流 + 生成语义流独立命中 + 主评审 grep 复核零引用 |
| ITM-105 | `DbOptions` record 合成 `ToString()` 打印 `ConnectionString`/`ReadConnectionString` 明文；且弃用 `ForRead()` 会把 `$ENV:` 间接引用物化为明文写回新实例。日志/异常/调试器场景泄露密码 | `src/PalORM.Core/DbOptions.cs:7-13`、`src/PalORM.Core/DataSession.cs:981-987` | ✅ | 安全流 Read + record 合成行为为语言事实 |
| ITM-106 | `ScalarAsync<T>` 类型不匹配静默返回 `default`：`result is T t ? t : default`——PG COUNT 返回 long、MySQL SUM 返回 decimal 时 `ScalarAsync<int>` 静默得 0。同文件 Max/Min 用 `Convert.ChangeType` 抛异常，行为不一致印证是遗漏 | `src/PalORM.Core/DataSession.cs:721` | ✅ | 错误流 Read + 主评审复读 |
| ITM-107 | PALORM002–007 六个诊断零负向测试（001/008–016 均有）；PALORM005 仅以 `#pragma warning disable` 形式在集成测试出现。违反本系统"诊断不退化必须有负向测试"强制项 | `src/PalORM.SourceGen/PalORMAnalyzer.cs:19-43`、`test/PalORM.SourceGen.Tests` | ✅ | 生成语义流全量核对测试文件 |
| ITM-108 | AUD-001 复核（在案 P0，用户决定仅记录）：本轮在当前 `git rev-list --all` 上重新验证——10 个提交中 8 个仍含真实凭据的旧版 `set-test-env.sh`（192.168.x.x 的 PG/MySQL 账密）。仓库侧当前版脚本已整改（从 `.env.test` 加载、不回显），G9 绿。待用户决策：历史重写 + 数据库侧凭据轮换 | git 历史 `b815ddf…f0df771` 共 8 提交 | ✅ | 主评审逐提交 git show 验证 |

### P2/P3 发现（代表项，全量见行动项清单）

| ID | 发现 | 位置 | 可信度 | 信息源 |
|:--:|------|------|:--:|--------|
| ITM-110 | SourceGen 无方言 legacy CRUD SQL（INSERT/UPDATE/DELETE/RETURNING）标识符未转义，经 `CommandSqls` 回退路径运行时可达（版本偏差场景）；在案 SEC-01 范围扩大 | `src/PalORM.SourceGen/CommandFactoryEmitter.cs:163-258`、`src/PalORM.Core/DataSession.cs:405-410` | ✅ | 安全流传播链逐行确认 |
| ITM-111 | Core 层方言泄漏：硬编码 `TProvider.Name == "SQLite"` PRAGMA 初始化；`!SupportsReturningClause` 即拼 MySQL 专有 SQL（`LAST_INSERT_ID`/`ON DUPLICATE KEY`）。第三方 Provider 无初始化钩子却收到 MySQL 语法 | `src/PalORM.Core/DataSession.cs:58-63,170,412-448` | ✅ | 架构流 + 主评审复读 |
| ITM-112 | `IDbProvider.ParameterPrefix`/`GetLimitOffsetClause` 零调用点（死接口成员）；`QueryBuilder.BuildLimitClause` 重复实现同职责；死代码中 MySQL `LIMIT 0, -1` 若被调用即非法 SQL | `src/PalORM.Core/IDbProvider.cs:16,34`、`src/PalORM.Core/QueryBuilder.cs:624-640`、`src/PalORM.MySql/MySqlProvider.cs:38-39` | ✅ | 架构流全库 grep + Read |
| ITM-113 | 设计决策 #12"BindInsertValues 零 DbCommand 批量路径"为死代码，三 Provider 实际每行建 rowCommand 提参——文档与实现相反 | `src/PalORM.Core/PalORM_Runtime.cs:57`、三 Provider BulkInsert | ✅ | 架构流 grep 零读取方 |
| ITM-114 | `QueryAsyncEnumerable` 门禁租约位于迭代器体内：调用方获取枚举器后不释放 → 租约永不退出，会话永久锁死且 `DisposeAsync` 无限挂起，无超时/终结器兜底 | `src/PalORM.Core/DataSession.cs:634-649` | ✅ | 资源流 + 并发流独立命中 |
| ITM-115 | CacheStore 过期条目不移除、无容量上限、无淘汰；过期项仍强引用整个列表（前序在案观察，现状未变，与 ITM-102 同文件不同缺陷） | `src/PalORM.Core/CacheStore.cs:8-29` | ✅ | 资源流 + 并发流独立命中 |
| ITM-116 | MySQL `InsertAsync` 不回读 DB 计算列：PG/SQLite 走 RETURNING 全列，MySQL 仅回填自增 ID——同一 API 跨方言返回不同数据，切库静默行为变化；测试用 GetAsync 重查规避未暴露 | `src/PalORM.Core/DataSession.cs:167-181` | ✅ | 生成语义流生成物+消费点双向核实 |
| ITM-117 | `QueryAsync<T>` 原生 SQL 按 ordinal 读取：`SELECT *` 或自定义列序与实体声明序不同且同型列时静默交换数据（用户 SQL 入口的 P0 型危害面，需契约文档化或列名映射） | `src/PalORM.Core/DataSession.cs:673-691`、`src/PalORM.SourceGen/RowFactoryEmitter.cs:26-31` | ✅ | 生成语义流机制核实 |
| ITM-118 | 熔断器半开态两处瑕疵：非探针最终失败清探针占用标志（可放行第二并发探针）；陈旧探针成功豁免 generation 检查 | `src/PalORM.Core/Resilience.cs:125,140` | ⚠ | 并发流完整锁块读取（静态推导） |
| ITM-119 | Tag 注释校验未拒绝 `/*`（PG 嵌套注释致语句语法错误）；`TagWithCaller` 将编译机绝对路径写入发往服务器的 SQL | `src/PalORM.Core/QueryBuilder.cs:305-307,642-648` | ✅ | 安全流 Read |
| ITM-120 | 清理异常聚合不一致：DataSession/SessionOperationState 用 `cleanupException ??=` 丢弃后续异常，GridReader/Provider 挂 `Exception.Data`——违反项目自身声明模式 | `src/PalORM.Core/DataSession.cs:1008-1029`、`src/PalORM.Core/SessionOperationState.cs:152` | ✅ | 错误流 + 资源流独立命中 |
| ITM-121 | 其余低危观察：SQLite PRAGMA 脱离取消保护（DataSession.cs:62）· 重试耗尽内部超时以裸 OCE 出面（Resilience.cs:55-79）· `100 << attempt` 无钳制（DataSession.cs:79）· StoredProcBuilder 参数实例跨命令复用二次执行失败（StoredProcBuilder.cs:77,96）· AddInterceptor 与在飞查询枚举竞态（DataSession.cs:459-468）· `tenant_id` 列名硬编码未 quote 且无编译期校验（DataSession.cs:131）· MySQL 方言 InsertReturningSql 含 RETURNING 潜伏错误 SQL（CommandFactoryEmitter.cs:249-258）· BuildUpdateSql 双实现独立演化风险（CommandFactoryEmitter.cs:179-228）· int/long 主键无条件自增静默丢弃用户值（TableModel.cs:52）· "Verify 快照测试"名不符实无真实快照 · Max/Min 的 `Convert.ChangeType` 对 Guid/DateOnly/枚举抛 InvalidCastException（DataSession.cs:574,582）· `StoredProc` 过程名无白名单校验 · 架构文档失实三处（stackalloc/文件清单/W3 签名，前序 ARCH-02/03/09/11 在案未修） | 各处 | ⚠/✅ | 各流 |

### 正面结论（已验证干净的面）

- **FormattableString 参数化全路径无值拼接**：Formatter 预校验格式、转义正确、全部转 DbParameter ✅
- **QueryBuilder/三 Provider/SAVEPOINT/LISTEN channel 标识符全部经 QuoteIdentifier（双写转义正确）** ✅
- **三序一致性（CREATE/INSERT 列序 = RowFactory 序号 = Bind 参数序）**：5 实体核对全一致，由"单一 model.Columns 源 + 同滤同序"构造性保证，无 P0 错列 ✅
- **77 个 catch 块逐个核对零 OCE 吞噬**；主异常保留模式在三 Provider/GridReader/ConnectionLease 贯彻正确 ✅
- **会话门禁覆盖完整**（全部 DataSession/Bulk/StoredProc/GridReader/QueryBuilder 执行入口逐方法核对）✅
- **运行时零反射面**（117 处 typeof 逐个定性均为字典键/类型恒等比较）、**零 IL 抑制**、OwnedJson 强制 JsonTypeInfo 源生成、ModuleInitializer 注册链 AOT 安全 ✅
- **资源释放主路径全净**：CreateAsync 重试环、WithTransaction、Bulk 三方法、PG Binary COPY、QueryMultiple 所有权移交、DisposeAsync 幂等性逐路径验证 ✅
- **AUD-001 仓库侧无恶化**：当前版脚本从未跟踪文件加载且不回显 ✅

---

## 段 4：优先级判定

| ID | 危害 | 复杂度 | 优先级 | 理由 |
|:--:|:--:|:--:|:--:|------|
| ITM-101 | 高 | 易（<1h） | **P0** | 静默返回错误数据（评审矩阵最高危害类）；SoftDelete/TenantAware 实体开箱即中；修复即"无条件复制或去掉标志"；且账本已标完成——账实不符本身是流程 P0 |
| ITM-102 | 高 | 易（<1h） | **P0** | 缓存污染跨调用方扩散 + 并发未定义行为；修复即存/取时复制列表 |
| ITM-103 | 中 | 易 | P1 | 公开 API 功能性损坏（fail loud 不损数据）；修复需 QuoteIdentifier 防注入 |
| ITM-104 | 中 | 中（1-4h） | P1 | 注解静默失效——用户以为建了索引/外键；正确修法含方言分支与诊断 |
| ITM-105 | 中 | 易 | P1 | 框架级凭据卫生缺口，需使用方动作触发；重写 ToString 脱敏即可 |
| ITM-106 | 中 | 易 | P1 | 静默错值（COUNT=0 是常见踩坑）；改为抛 InvalidCastException 或 ChangeType |
| ITM-107 | 中 | 中 | P1 | 诊断回归无守护，违反本系统强制项 |
| ITM-108 | 高 | 难（需用户决策） | P0 在案 | 凭据仍在 8 个历史提交；按既定决定仅记录，G9 持续阻断 |
| ITM-110 | 低 | 中 | P2 | 需版本偏差才可达；纵深防御缺口 |
| ITM-111/112/113 | 中 | 中 | P2 | 架构债：方言泄漏/死接口/文档与实现相反 |
| ITM-114 | 中 | 中 | P2 | 需调用方违反枚举器契约；但后果是无超时永久挂起 |
| ITM-115 | 低 | 易 | P2 | 过期即删一行改动 + 容量上限需设计 |
| ITM-116/117 | 中 | 中 | P2 | 跨方言语义分叉/用户 SQL 契约需文档化 + 测试补强 |
| ITM-118/119/120 | 低 | 易-中 | P2/P3 | 窄窗口竞态/可用性/可观测性 |
| ITM-121 各项 | 低 | 易-中 | P3 | 逐项见行动项清单 |
| GEN-06→ADR | — | — | 评估 | QueryAsync 按序读取是设计契约还是缺陷需 ADR（列名映射 vs 文档声明） |

---

## 段 5：方法论自省

**工具选择**：
- 结构发现：`review-snapshot.sh` + find/git log + 架构文档对照
- 代码审查：7 个并行子代理（每流强制加载 `.ai/audit-system/known-false-positives.md` 15 条误判模式），逐文件读取完整方法体
- 交叉验证：主评审对全部 P0/P1 逐项二次 Read + grep；机械验证走 `gate-check.sh`（25/25）、`verify-ai-system.sh`（9/9）、`doc-consistency-check.sh`（8/8）、三套 dotnet test 实测
- AUD-001 复核：`git rev-list --all` 逐提交 `git show` 重新验证（误判模式 P8 要求）

**已知局限**：
- 并发/竞态发现基于静态推导，未编写复现测试（CONC-01 场景 B/C 逻辑确定性成立故标 ✅，其余窄窗口标 ⚠）
- 子代理审计存在重叠命中（同一缺陷多流报告），已由主评审合并去重；不排除各流盲区交界处漏项
- 未运行 BenchmarkDotNet，性能声明未复核

---

## 段 6：跨报告一致性

前序报告：`.ai/audit-system/reports/audit-2026-07-17.md`（86.5 分）、`.ai/review-system-v2/reports/review-2026-07-11-56e3cbc.md`

| 前序结论 | 本轮核验 |
|---|---|
| ARCH-04/QUERY-001"账本待实施" → 账本后标"已完成"（c024fa3） | **推翻账本**：仅修复场景 A，场景 B/C 残留（ITM-101）——这是本轮最重要的跨报告发现 |
| 审计称"并发流零缺陷 9.5/10" | 本轮并发流发现 2 项 P0 级（ITM-101/102）——前轮并发流覆盖度虚高，视角账本已反映其发现率下滑 |
| SEC-01 死代码缺口 | 确认范围扩大至运行时可达 fallback（ITM-110） |
| ERR-01 FormatException 缺索引 | 仍未修复，维持在案 |
| CacheStore 过期不清理观察 | 现状未变（ITM-115），且新增共享列表缺陷（ITM-102） |
| AUD-001"仓库侧已整改" | 确认无恶化；历史 8 提交凭据残留与账本追加记录一致 |
| 审计综合 86.5 分 | 本轮发现 2 项 P0 后该分数应视为高估——审计的"零 P0"结论被推翻，建议下轮审计将「缓存共享引用」「struct 写时复制完备性」加入视角池 |

---

## 段 7：建议与修复方案

### P0 — 紧急（合并前必须修）
| ID | 问题 | 修复方案 | 预估时间 |
|:--:|------|---------|:--:|
| ITM-101 | QUERY-001 残留 | 方案 A（推荐）：`AddClause` 每次写入无条件复制（删除 `_writable` 短路）；方案 B：复制检测改为快照计数比对。补测试：已写入 builder 复制分支 + SoftDelete/TenantAware 实体 `From<T>()` 后复制分支 | 1h |
| ITM-102 | 缓存共享可变列表 | `CacheStore.Set` 存入 `new List<T>(list)` 快照，`TryGet` 命中返回 `new List<T>(cached)`；或改存 `IReadOnlyList<T>` 并变更返回类型（binary-breaking，留 3.0） | 1h |

### P1 — 近期（本迭代内修）
| ID | 问题 | 修复方案 | 预估时间 |
|:--:|------|---------|:--:|
| ITM-103 | WhereJson 损坏 | 重写为 `builder.Where($"{qi(col)}->>{path} = {value}")` 形态：列名经 QuoteIdentifier、path 作参数绑定；补 PG 集成测试 | 1h |
| ITM-104 | Index/FK/DefaultValue 静默失效 | 短期：MigrateAsync 消费 CreateIndex 常量（SQLite FK 改内联列定义）+ FK 列名走映射名；或移除注解并发 PALORM 诊断"暂不支持"。长期需 ADR | 3h |
| ITM-105 | 连接串明文泄露面 | `DbOptions` 重写 `ToString()` 脱敏两个连接串属性；`ForRead()` 弃用路径不物化 `$ENV:` | 0.5h |
| ITM-106 | ScalarAsync 静默 default | 与 Max/Min 对齐：非匹配时 `Convert.ChangeType` 或抛 `InvalidCastException`（含实际类型名） | 0.5h |
| ITM-107 | 诊断零负向测试 | 为 PALORM002-007 各补 1 个触发 + 1 个不级联生成错误断言 | 2h |
| ITM-108 | AUD-001 | 待用户决策：本地历史重写（8 提交，无 remote）+ 192.168.x.x 凭据轮换 | 用户 |

### P2 — 计划（下个迭代）
| ID | 问题 | 修复方案 |
|:--:|------|---------|
| ITM-110 | legacy CRUD SQL 未转义 | 删除无方言重载，回退路径改抛明确异常 |
| ITM-111 | Core 方言泄漏 | `IDbProvider` 增 `InitializeConnectionAsync` 钩子 + Insert 方言策略下沉 |
| ITM-112 | 死接口成员 | 删除 `ParameterPrefix`/`GetLimitOffsetClause` 或让 BuildLimitClause 消费之（二选一，binary-breaking 留 3.0） |
| ITM-113 | BindInsertValues 死代码 | 删除注册链 + 修正架构设计.md 决策 #12 |
| ITM-114 | 枚举器租约锁死 | 租约增超时诊断或文档强制 `await foreach`；`SessionOperationState.DisposeAsync` 加超时报错 |
| ITM-115 | CacheStore 无淘汰 | TryGet 过期即 TryRemove + Set 容量上限 |
| ITM-116 | MySQL Insert 不回读 | 文档声明差异或 MySQL 分支补 SELECT 回读 |
| ITM-117 | QueryAsync 按序读取 | 文档显式契约 + 考虑列名映射 RowFactory 重载（评估项联动） |
| ITM-118/119/120 | 熔断半开/Tag 校验/清理聚合 | 逐项小修，见行动项清单 |

### 评估 — 需 ADR
| ID | 问题 | 需要决策 |
|:--:|------|---------|
| ADR-A | QueryAsync 原生 SQL 列序契约 | 按序读取（现状，快但危险）vs 列名映射（安全但慢）vs 双模式 |
| ADR-B | Index/FK 迁移支持范围 | 完整实现（含 SQLite 方言限制处理）vs 移除注解明确不支持 |
| ADR-C | 会话级缓存 vs 进程级静态缓存 | CacheStore 静态设计与"零全局可变状态"清单冲突的最终裁决 |

---

## 段 8：自我局限声明

**我确认**：
- [x] 已运行 `scripts/review-snapshot.sh` 并粘贴输出
- [x] 已明确声明评审范围和不评审范围
- [x] 已声明抽样策略（src/ 全量逐行，七流并行）
- [x] 所有 P0/P1 发现至少有 2 个独立信息源验证（子代理逐行读取 + 主评审二次 Read/grep/git 实证）
- [x] 已排除 `*.g.cs` 源生成器生成文件（仅验证生成语义）
- [x] 盲区已在段 1 明确声明

**我已知的局限**：
- PG/MySQL 真实数据库行为未验证（本机无凭据，4 项集成测试环境性失败）；AOT 原生运行本轮未重新执行
- 并发发现为静态推导，未做压测或复现测试
- 七流子代理由 LLM 执行，误判知识库已强制加载，但不能排除新型误判；P0/P1 已全部由主评审独立复核，P2/P3 部分依赖单流结论（已标 ⚠）
- 性能声明（~1.11× Raw ADO）未复核

---

## 事后追加：P0/P1 实施记录（2026-07-17 晚）

> 按「报告不可变」规则以追加区块记录，不改写正文结论。

| ID | 实施 | 验证 |
|:--:|------|------|
| ITM-101 | `AddClause` 无条件写时复制（删除 `_writable` 一次性标志）；`EnsureWritable` 移除 | 新增 4 个场景 B/C 用例；反向验证：撤回复制逻辑后用例确定性失败；Core 124/124 |
| ITM-102 | `Set` 存快照副本、`TryGet` 命中返回副本；CacheStore 过期条目读取时 `TryRemove`（顺带收口 ITM-115 的过期驻留半项） | 缓存污染防护集成测试 + CacheStore 过期移除测试通过 |
| ITM-103 | `WhereJson` 重写为 `(column, path, value)`：列名 QuoteIdentifier（含花括号转义防格式解析）、path/value 绑定参数 | 2 个 SQL 生成测试（含 `"` 双写转义）；文档 PG1 同步 |
| ITM-104 | 删除零消费者的 CreateIndex_*/FK_* 死常量；新增 PALORM017 编译期告警覆盖 [Index]/[Unique]/[ForeignKey]/[DefaultValue]/[Column 架构参数]——静默失效改为编译期显式告知 | 全解决方案 0 warning（现有实体无违规注解）；文档 M4/M6/BA11 标注；完整 DDL 支持留 ADR-B |
| ITM-105 | `DbOptions.ToString()` 覆写脱敏两个连接串；弃用 `ForRead()` 不再物化 `$ENV:`（CreateAsync 内部按需解析） | 2 个脱敏测试；G9 门禁保持绿（测试内凭据字面量改用 example- 前缀白名单） |
| ITM-106 | `ScalarAsync<T>` 类型不匹配改 `Convert.ChangeType`（InvariantCulture，Nullable 解包），与 Max/Min 对齐；无法转换抛异常不再静默 default | 5 个转换测试（long→int、double→decimal、NULL、不可转换抛 FormatException） |
| ITM-107 | 新增 AnalyzerDiagnosticsTests：PALORM002/003/004/005/017 各有触发 + 负向 + 不级联生成错误断言；PALORM006/007 无 Analyzer 报告点的现状以固化测试记录 | SourceGen 62/62 |
| ITM-108 | 不实施（按在案决定仅记录，待用户决策历史重写 + 凭据轮换） | G9 持续阻断能力保留 |

**最终验证基线**：Release 严格构建 0 warning/0 error · Core 124/124 · SourceGen 62/62 · Integration 122/126（4 项 PG/MySQL 凭据缺失环境性失败，与修复无关）· gate-check 25/25 · doc-consistency 8/8 · verify-ai-system 9/9 · verify-action-items 50/50。

**账本回改**：`docs/review/implementation-tasks-2026-07-15.md` QUERY-001 更新为「已完成（二次修复）」并记录一次性标志失效根因。

---

## 事后追加 2：P2/P3 实施记录（2026-07-17 深夜）

| ID | 实施摘要 |
|:--:|---------|
| ITM-110 | `GetCommandSqls` 回退路径改抛明确异常（要求旧生成器程序集重新编译），未转义 legacy SQL 不再运行时可达 |
| ITM-111 | `IDbProvider.InitializeConnectionAsync` 钩子（SQLite PRAGMA 下沉 Provider）；Insert/Upsert 无 RETURNING 分支加 `Dialect != MySql` 显式守卫 |
| ITM-112 | `ParameterPrefix`/`GetLimitOffsetClause` 标注 `[Obsolete]`（3.0 移除）；MySQL 死代码 `LIMIT 0,-1` 缺陷修复（极大值哨兵与 BuildLimitClause 对齐） |
| ITM-113 | BindInsertValues 全链删除（Emitter/Registry/RegistryFragment/Runtime 快照）；架构设计决策 #12 改写为已删除 |
| ITM-114 | `SessionOperationState` 释放等待加 5 分钟上限（`WaitAsync`），超时抛指向"被放弃枚举器"的诊断异常 |
| ITM-115 | CacheStore 过期条目读取时 TryRemove（容量上限归 ADR-C） |
| ITM-116 | InsertAsync 文档声明 MySQL 不回读 DB 计算列的跨方言差异 |
| ITM-117 | QueryAsync 文档声明 ordinal 列序契约（强化方案归 ADR-A） |
| ITM-118 | 熔断半开态：仅探针自身失败/完成释放探针占用标志；generation 防陈旧对探针一体生效（补三方竞态测试） |
| ITM-119 | ValidateSqlComment 同时拒绝 `/*`；TagWithCaller 文档标注路径泄露 |
| ITM-120 | DataSession/SessionOperationState 清理异常统一为"首异常主抛 + 后续挂 Exception.Data" |
| ITM-130 | SQLite PRAGMA 经 InitializeConnectionAsync 纳入连接超时/取消保护（并 ITM-111） |
| ITM-131 | 重试耗尽的内部超时包装 `TimeoutException`（inner 保留原 OCE）；既有测试断言同步 |
| ITM-132 | CreateAsync 兜底退避复用 `ResilienceExecutor.GetDefaultBackoff`（含移位钳制） |
| ITM-133 | StoredProcBuilder 一次性契约：二次执行提前抛明确异常 |
| ITM-134 | AddInterceptor 纳入会话操作门禁，与在飞查询互斥 |
| ITM-135 | tenant_id 过滤列名经 QuoteIdentifier；新增 PALORM018（TenantAware 必须映射 tenant_id 列） |
| ITM-136 | MySQL 方言 InsertReturningSql 生成空串（误用以空 CommandText 明确失败，不发非法 SQL） |
| ITM-137 | BuildUpdateSql 双实现统一为单方法（legacy 委托 dialect:null）；SourceGen 测试断言同步 |
| ITM-138 | `[Key(AutoIncrement = false)]` 关闭数值主键自增推断（雪花 ID 场景显式赋值进 INSERT 列） |
| ITM-139 | "Verify 快照测试"改名结构断言；移除零使用的 Verify.TUnit/Verify.SourceGenerators 包引用 |
| ITM-140 | Max/Min 文档声明 IConvertible 类型限制 |
| ITM-141 | StoredProc 过程名白名单校验（字母/下划线开头 + 点分限定符） |
| ITM-142 | 架构文档三处失实修正（stackalloc→ArrayPool、文件清单、W3 签名）；决策 #12 同步 |
| ITM-143 | SqlFileEmitter 补 IOException/UnauthorizedAccessException 诊断分支 |
| ITM-144 | `_options` 改 Volatile.Write 与 `_resilience` 保护对齐 |
| ITM-145 | GridReader DisposeAsync 文档化调用方契约与等待语义（超时机制由 ITM-114 的会话级兜底覆盖） |
| ITM-146 | PgNotificationListener 释放权移交协议：Stop 先清引用声明接管，Run finally 仅在仍持有引用时 Dispose——消除 CTS Dispose 与 CancelAsync 并发 |
| ITM-147 | ToPageAsync 未登记事务的自包含前提代码注释固化 |
| ITM-148 | QueryBuilder 四处注册表索引器直取改 TryGetValue + 含类型名异常 |
| ITM-149 | 三个 Build 方法 try/finally 保证 ArrayPool 归还（Dispose 幂等） |
| ITM-150/151/152 | ADR-A/B/C 草案已出（docs/adr/），含选项矩阵与推荐，待用户批准后实施 |

**验证基线**：Release 严格构建 0W0E · Core 127/127 · SourceGen 62/62 · Integration 122/126（4 项环境性）· gate 25/25 · doc-consistency 8/8 · verify-ai-system 9/9 · SQLite Native AOT publish 0 IL 警告 + 原生运行 PASS。

**Binary-breaking 注意**：RegistryFragment 删除 BindInsertValues 成员 + KeyAttribute 新增属性 + IDbProvider 新增虚成员——旧版本生成器产物与已编译消费者需重新编译（与 2.0.0 变更先例同性质，建议记入变更日志作为 2.1.0/3.0 项）。
