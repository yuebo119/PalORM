# PalORM 全面评审报告：70fc6ec（整改验证轮）

> 评审日期：2026-07-18
> 评审范围：三轮整改增量（27da9ca / 3774877 / 70fc6ec 相对 90c2039）+ 12 项回归面核对
> 评审方法：四组联合审计流（架构+生成语义 / 安全+错误 / 并发+资源 / AOT+回归）全量逐行
> 评审基线：`70fc6ec64f2b58b1fba42a63da75c10e254cc90c`（dev）
> 结论：**REQUEST_CHANGES** —— 0 项 P0、2 项 P1（MySQL 索引 DDL 不可执行 + ADR-A 校验覆盖缺口）、若干 P2/P3。前轮 41 项整改与 3 项 ADR 实施质量总体良好，12 项回归核对全部通过无回退。

---

## 段 1：范围与方法

**评审范围**：
- 三轮整改触及的全部 src/ 文件（22 个）逐行审计，聚焦新增/变更代码
- 新增 5 个测试文件、生成物（`Migration_*.g.cs`/`PalORM_Registry.g.cs`）语义验证
- 回归面：前轮 12 项已修项（REG-a~l）在当前 HEAD 逐项 Read 验证

**明确不评审**：
- 90c2039 之前既有未变更代码（前轮已全量评审）
- `*.g.cs` 风格（仅验语义）；性能 profiling；真实 PG/MySQL 运行（本机无凭据）

**抽样策略**：整改增量全量逐行（`git diff -w 90c2039..70fc6ec` 驱动），四组流并行。

**盲区声明**：
- MySQL 1170/1061 行为、Npgsql 对 NUL 的异常形态、PG `text = integer` 报错均为文档化行为推导，无真库实测
- 并发发现（探针活性、缓存 TOCTOU）为静态推演，未压测
- `dotnet publish AOT` 本轮未重新执行（AOT 结论基于静态分析 + 历史实测）
- SqlFileEmitter 大改段（约 400 行 diff 中的既有逻辑重排）未逐行复审

---

## 段 2：评审基线

```
═══════ 评审基线快照 ═══════
时间：2026-07-18 00:24:53 CST
分支：dev
提交：70fc6ec64f2b58b1fba42a63da75c10e254cc90c
SDK：11.0.100-preview.6.26359.118
工作树：有未提交变更（git diff --ignore-cr-at-eol 为空——仅行尾符噪音 + 未跟踪 .depwire/mydatabase.db）

─── 文件统计 ───
C# 源文件：40 · 测试文件：32 · 文档文件：12

─── 构建状态 ───
    0 Warning(s)  0 Error(s)  构建退出码：0
```

**本轮机械验证实测**：gate-check 25/25 · stub-check PASS · verify-ai-system 9/9 · doc-consistency 8/8 · Core 137/137 · SourceGen 64/64 · Integration 125/129（4 项 PG/MySQL 凭据缺失，环境性）。

---

## 段 3：准确性分析

### P1 发现（✅ 双源验证）

| ID | 发现 | 位置 | 可信度 | 信息源 |
|:--:|------|------|:--:|--------|
| ITM-201 | **MySQL 方言对 TEXT 列生成的索引 DDL 无法执行**：非主键 string 列映射为 `TEXT`（仅主键特判 VARCHAR(255)），而 MySQL 对 TEXT/BLOB 列建索引必须指定前缀长度——报错 1170，非 1061，`IsDuplicateSchemaObject` 不吞。IndexedProduct 生成物实测确认：`CREATE UNIQUE INDEX \`ux_indexed_products_sku\`` 而同 DDL 中 `sku TEXT`。ADR-B 的核心交付在 MySQL 上对最常见的字符串唯一键（SKU/email/username）**首次迁移即抛异常**。SQLite 集成测试全绿掩盖此点，MySQL 无索引迁移用例 | `src/PalORM.SourceGen/MigrationEmitter.cs:131-174`（GetDbType）、`:55-66`（BuildCreateIndex） | ✅ | 生成语义流推导 + 主评审生成物实测（TEXT 列型与索引 DDL 并存）+ MySQL 文档化行为 |
| ITM-202 | **ADR-A 列序校验未覆盖 GridReader 与 StoredProcBuilder**：`QueryMultipleAsync` 的 `GridReader.ReadAsync`/`ReadFirstAsync` 与 `StoredProcBuilder.QueryAsync` 同为"调用方原生 SQL/存储过程 + ordinal 映射"，但未接 `ValidateColumnOrder`——同型列错位仍静默交换数据，正是 ADR-A 要消灭的危害面，只堵了 QueryAsync 一族 | `src/PalORM.Core/GridReader.cs:34-45,72-75`、`src/PalORM.Core/StoredProcBuilder.cs:102` | ✅ | 并发资源流 Read + 主评审核对 ValidateColumnOrder 调用点仅 2 处（QueryAsync/QueryAsyncEnumerable） |

### P2 发现

| ID | 发现 | 位置 | 可信度 | 信息源 |
|:--:|------|------|:--:|--------|
| ITM-203 | 索引名冲突静默化三联：① `ux_{table}_{column}` 下划线拼接无歧义边界（表 `user_settings` 列 `id` 与表 `user` 列 `settings_id` 同名），SQLite/PG 的 IF NOT EXISTS 使第二个实体的唯一索引**静默不建**；② MySQL 1061 幂等兜底把"同名异构索引"也归类为已建过静默跳过；③ `[Index("name")]` 空列声明与同实体重名索引被 TableModel 静默丢弃——三者同根：名字冲突/无效声明走静默路径，违背 PALORM017 自身确立的"消除静默失效"哲学 | `src/PalORM.SourceGen/TableModel.cs:55,127-134`、`src/PalORM.Core/DataSession.cs:580-583` | ✅/⚠ | 架构+生成语义流（③ 双源，①② 机制推导） |
| ITM-204 | `CreateIndexSqlSet` 持有裸数组引用：Register 对 ColumnNames 有防御性克隆纪律（`Clone()+AsReadOnly`），对 CreateIndexSqlByDialect 经泛型 Merge 直传生成代码的 `static readonly string[]`——`Get()` 返回值可向下转型修改，与"注册表完整不可变"声明不符 | `src/PalORM.Core/PalORM_Runtime.cs:156-157,256-270` | ✅ | 架构流双源（Merge 路径 + 生成物数组声明） |
| ITM-205 | DbOptions 脱敏不覆盖序列化器/调试器路径：ToString 覆写只保护字符串插值/日志/异常消息；Serilog `{@Options}` 解构、STJ 序列化、调试器属性面板仍输出连接串明文（record 合成路径已确认封死：PrintMembers 无调用方、with 共享覆写） | `src/PalORM.Core/DbOptions.cs:10,112-118` | ✅ | 安全流 + C# record 语义验证 |
| ITM-206 | CreateAsync 连接超时耗尽后抛裸 OCE：`cts.CancelAfter(ConnectionTimeout)` 的 OCE 在末次尝试直接上抛，调用方无法区分"连接超时"与"我被取消"——命令路径已包装 TimeoutException（ITM-131 修复），连接路径漏了对称处理 | `src/PalORM.Core/DataSession.cs:53-55,66` | ✅ | 错误流过滤器条件直读 |
| ITM-207 | InitializeConnectionAsync 未覆盖读路由连接：接口文档暗示会话所有连接经初始化，但 `ForRead()` 读路由经 `ConnectionLease.OpenOwnedAsync` 打开不调钩子。SQLite 现状实害趋零（FK 检查只读无关、WAL 持久于文件头），但属钩子契约未声明例外——未来 Provider 在钩子做会话级 SET 时读路由静默缺失 | `src/PalORM.Core/IDbProvider.cs:54-57`、`src/PalORM.Core/ConnectionLease.cs:22-39` | ✅ | 架构流双源（接口文档 + ConnectionLease 路径） |

### P3 观察（代表项）

| ID | 发现 | 位置 | 可信度 |
|:--:|------|------|:--:|
| ITM-208 | ScalarAsync/Max/Min 对 Guid/枚举目标抛 InvalidCastException（`Guid.Parse` 本可成功）；Max/Min 已文档化，ScalarAsync 文档只提数值场景 | `DataSession.cs:817` | ✅ |
| ITM-209 | 熔断活性：陈旧操作失败使在飞探针的成功因 generation 不匹配作废，熔断多保持一个周期（保守方向，无死锁——探针占用标志四出口释放已穷举验证） | `Resilience.cs:137-157` | ⚠ |
| ITM-210 | BoundedQueryCache：Set 的 Count 检查 TOCTOU 软超容（≤并发度，有界）+ `ConcurrentDictionary.Count` 全分段锁；文档未声明"软上限" | `CacheStore.cs:54-59` | ✅ |
| ITM-211 | ValidateColumnOrder 用 `min(FieldCount, expected.Count)` 截断：结果列数少于实体列数时超出部分不校验（RowFactory 读取时会自然抛 IndexOutOfRange，非静默，但校验语义不完整） | `DataSession.cs:767-785` | ⚠ |
| ITM-212 | WhereJson：`\0` 未在库内拒绝（失败关闭依赖驱动，与 ValidateSqlComment 防御不对称）；非字符串 value 与 `->>` text 结果比较抛 PG 类型错误（文档声明"按 text 比较"但不归一） | `PostgreSqlExtensions.cs:14-22` | ⚠ |
| ITM-213 | MigrateAsync 三次独立 Volatile 读注册表快照，理论撕裂读窗口（ModuleInitializer 时序下实际窗口极窄） | `DataSession.cs:555-567` | ⚠ |

### 回归面核对（REG-a~l 全部通过 ✅）

12 项前轮修复逐项 Read 当前代码验证无回退：AddClause 无条件复制（零 `_writable` 残留）· 缓存双向副本 · WhereJson 三参签名 · ToString 脱敏 · TimeoutException 包装 · tenant_id quote · ValidateSqlComment 拒 `/*` · GetRegisteredTableName · MySQL 方言守卫 ·  legacy SQL 回退已改明确异常（ITM-110，fallback 死参 + 重编译指引）· BindInsertValues 已删（含防回退守护断言）· 死接口成员已标注 Obsolete/3.0 移除（ITM-112）。

### 正面结论

- **AOT 面增量零违规**：新增 typeof 约 60 处全部为注册表查键/诊断消息；Convert.ChangeType 走 IConvertible 无 AOT 标注需求；static virtual 成员编译时分发；IL 抑制增量 0；NoWarn 增量 0
- **WhereJson 注入面完整**：`"`双写、花括号转义、未配对 `{` 失败关闭、path/value 全绑定——六种特殊字符逐一推演无逃逸
- **Resilience 三 catch 互斥完备**：包装分支不吞调用方取消（TOCTOU 仅存在保守方向）；探针占用标志成功/失败/取消/超时四出口全释放，无死锁
- **NormalizeGeneratedId** null 臂在 throw 臂前，无 NRE；**RecordCleanupException** ref 语义正确、键序无覆盖
- **MigrateAsync**：租约贯穿、每命令 await using 释放、1061 过滤器精确（1072/1146/语法错误穿透）
- **77+8 个 catch 块**（前轮 77 + 本轮新增 8）OCE 过滤全部合规

---

## 段 4：优先级判定

| ID | 危害 | 复杂度 | 优先级 | 理由 |
|:--:|:--:|:--:|:--:|------|
| ITM-201 | 高 | 中（1-4h） | **P1** | MySQL 上 ADR-B 交付对字符串索引列直接不可用，fail loud（首迁即抛）非静默——但这是新功能在三方言之一的功能性缺陷，CI 服务容器跑通前不应宣布 ADR-B 完成 |
| ITM-202 | 中 | 中 | **P1** | 静默数据错位是本评审系统定义的最高危害类；GridReader/存储过程是 ADR-A 明确范围的自然延伸 |
| ITM-203 | 中 | 中 | P2 | 唯一约束静默丢失是数据完整性问题；建议合并为一个 Analyzer 诊断（空列/重名 [Index] 报错）+ 吞 1061 处日志 |
| ITM-204/205/206/207 | 低-中 | 易-中 | P2 | 纪律对齐/凭据卫生残面/对称性/契约声明 |
| ITM-208~213 | 低 | 易 | P3 | 文档化或小修 |

---

## 段 5：方法论自省

**工具选择**：结构发现 `git diff -w 90c2039..70fc6ec` 驱动；四组联合子代理（每组两流，均强制加载误判知识库）；主评审对 P1 逐项二次验证（ITM-201 生成物实测、ITM-202 调用点核对）；机械验证四脚本 + 三测试套件实测。

**已知局限**：两组代理输出经转录恢复（会话超时重发），内容完整性已核对；GEN-01 的 1170 行为置信度受限于无 MySQL 实测（生成物 TEXT 列型 + MySQL 文档行为双源，但非运行验证）。

---

## 段 6：跨报告一致性

前序报告：`review-2026-07-17-90c2039.md`（42 项行动项）

| 对比项 | 结论 |
|---|---|
| 前轮 41 项整改质量 | 12 项回归核对全过，核心修复（快照隔离/缓存副本/探针标志/钩子释放）四流独立定性正确——整改质量良好 |
| 前轮评审遗漏 | ITM-202（GridReader/StoredProc 的 ordinal 映射面）在前轮 GEN-06/ADR-A 讨论中未识别为同类危害面——属前轮盲区，本轮补正 |
| 新功能引入新缺陷 | ITM-201/203 均由 ADR-B 新代码引入——符合"新功能需新一轮评审"的流程预期，SQLite-only 测试矩阵是根因（PG/MySQL 无凭据）|
| ITM-108（凭据） | 状态不变，仍待用户决策，G9 持续阻断 |

---

## 段 7：建议与修复方案

### P1 — 近期（本迭代内修）
| ID | 问题 | 修复方案 | 预估时间 |
|:--:|------|---------|:--:|
| ITM-201 | MySQL TEXT 索引列 | 方案 A（推荐）：MySQL 方言下被索引的 string 列建列改 `VARCHAR(255)`（与主键特判对齐，TableModel 需知道哪些列被索引）；方案 B：生成前缀索引 `(col(191))`；两案均补 MySQL 生成物断言 | 2-3h |
| ITM-202 | 校验覆盖缺口 | ValidateColumnOrder 下沉为共享静态（或复制到 GridReader/StoredProcBuilder），GridReader 每结果集首行校验、StoredProc 查询首行校验；补同型列交换测试 | 2h |

### P2 — 计划
| ID | 修复方案 |
|:--:|---------|
| ITM-203 | 新增 PALORM018：空列 [Index] 报错、同实体重名索引报错、跨实体 ux_ 命名碰撞告警；MigrateAsync 吞 1061 处走 ILogger 警告 |
| ITM-204 | Register 时 `Array.AsReadOnly(list.ToArray())` 包装，与 ColumnNames 纪律对齐 |
| ITM-205 | DbOptions XML 文档标注"勿整体序列化/解构"；可选提供 Redacted() 视图 |
| ITM-206 | CreateAsync 末次连接超时包装 TimeoutException（与 Resilience 对称） |
| ITM-207 | ConnectionLease.OpenOwnedAsync 补调钩子，或接口文档明示"仅主连接" |

### P3 — 低危
ITM-208 文档补 ScalarAsync 类型支持范围 · ITM-209/210 注释声明取舍（软上限/保守恢复）· ITM-211 校验补列数不足分支 · ITM-212 WhereJson 拒 `\0` + value 归一或文档 · ITM-213 不动（窗口极窄）。

---

## 段 8：自我局限声明

**我确认**：
- [x] 已运行 `scripts/review-snapshot.sh` 并粘贴输出
- [x] 已明确声明评审范围（整改增量 + 回归面）和不评审范围
- [x] 已声明抽样策略（增量全量逐行）
- [x] P1 发现均 2 个独立信息源（ITM-201 生成物实测 + MySQL 文档行为；ITM-202 代码路径 + 调用点核对）
- [x] 已排除 `*.g.cs`（仅验语义）
- [x] 盲区已在段 1 声明

**我已知的局限**：MySQL/PG 真库行为未实测（1170/1061/text 比较均为文档推导）；并发结论未压测；AOT publish 未重新执行；两组代理输出经转录恢复。
