# PalORM 全面评审报告：9e23c90（全量深度轮）

> 评审日期：2026-07-18
> 评审范围：src/ 全部 6 项目 + test/ 9 项目 + docs 口径 + 门禁脚本
> 评审方法：四组联合审计流（Core 运行时 / SourceGen 生成语义 / 三 Provider 方言 / 测试与文档）全量逐行
> 评审基线：`9e23c9070e283d2ed6b46c6970969f8b720a18dc`（dev）
> 结论：**REQUEST_CHANGES** — 1 项 P0（TimeOnly 生成不可编译代码，编译探针实证）、4 项新 P1 + 2 项前轮遗留 P1、13 项 P2。架构方向、资源生命周期、SQL 参数化、AOT 纪律显著高于同类开源微 ORM；主要风险集中在多租户过滤覆盖断裂与白名单类型未经端到端验证。

---

## 段 1：范围与方法

**评审范围**：
- src/PalORM.Core（21 文件 4,499 行）逐行；src/PalORM.SourceGen（13 文件）逐 Emitter + 真实生成物比对 + 编译探针；src/PalORM.{Sqlite,MySql,PostgreSql,Testing} 全量 + 接口对称性对照表
- test/ 断言强度抽读 8 文件 + 测试方法静态计数 + Core.Tests 实际运行
- docs 口径数字全部实测复核；gate-check / verify-ai-system / doc-consistency 实际执行

**明确不评审**：
- `*.g.cs` 风格（仅验语义）；bench/ 性能基准未运行；tools/PalORM.Scaffold
- Integration.Tests 与 SourceGen.Tests 未实际运行（协调方中止），其通过数仅经静态计数 + 文档口径交叉验证

**抽样策略**：src 全量逐行；test 断言强度按代表性抽读约 25%。

**盲区声明**：
- MySQL 1170/DECIMAL 截断、Npgsql COPY 类型推断、SQLitePCL AOT 初始化均为文档化行为推导，无真库/真 AOT 发布实测
- 并发发现为静态推演未压测；serena MCP 本会话不可用，代码发现走 Task 子代理 + Grep 回退链

---

## 段 2：评审基线

```
时间：2026-07-18 01:15:40 CST · 分支：dev · 提交：9e23c90
SDK：11.0.100-preview.6.26359.118 · 工作树：有未提交变更（33 文件 +3479/-3479，全量对称，实质为行尾符噪音）
机械验证实测：build 0 警告 0 错误 · gate-check 25/25 · verify-ai-system 9/9 ·
doc-consistency 8/8 · Core.Tests 137/137 通过（dotnet run 实测）
```

---

## 段 3：准确性分析（发现清单）

### P0

| ID | 发现 | 位置 | 可信度 | 信息源 |
|:--:|------|------|:--:|--------|
| ITM-301 | **TimeOnly 属性生成不可编译代码**：RowFactoryEmitter 对 TimeOnly 生成 `r.GetTimeSpan(ordinal)`，但 `DbDataReader` 无此实例方法（net11.0 下 CS1061，/tmp 最小工程编译探针实证）。TimeOnly 在 PALORM016 白名单且 MigrationEmitter 为其生成 DDL——含 TimeOnly 实体的消费方项目直接编译失败，错误落在生成文件里难定位。全仓库无 TimeOnly 测试实体故从未暴露 | `src/PalORM.SourceGen/RowFactoryEmitter.cs:97-98` | ✅ | 编译探针 + 白名单/DDL 双路径核对 |

### P1（新发现）

| ID | 发现 | 位置 | 可信度 | 信息源 |
|:--:|------|------|:--:|--------|
| ITM-302 | **TenantAware 过滤仅覆盖 `From<T>()`，直连 CRUD/聚合全部绕过**：GetAsync/GetAllAsync/UpdateAsync/DeleteAsync/Count/Sum/Max/Min/Avg 只附加软删条件不附加 `tenant_id`——`WithTenant()` 会话仍可按主键跨租户读/改/删，GetAllAsync/CountAsync 返回全租户数据。软删在同入口都做了而租户没做，判定为遗漏而非设计。多租户场景 = 数据泄露 | `src/PalORM.Core/DataSession.cs:221-325,591-640,1145-1151` | ✅ | Core 流与 SourceGen 流独立发现（双源）+ 生成侧 EntityFeatures 落表正确、消费侧断裂的链路核对 |
| ITM-303 | **MySQL 方言 decimal 映射为裸 `DECIMAL`**：MySQL 裸 DECIMAL 即 DECIMAL(10,0)，写入 12.34 存成 12，金额字段静默截断；SQLite NUMERIC 亲和性亦有精度风险，仅 PG 安全 | `src/PalORM.SourceGen/MigrationEmitter.cs:144` | ✅ | 方言流 + MySQL 文档化行为 |
| ITM-304 | **BulkInsertAsync 约 200 行 SQLite/MySQL 逐字复制且已发生行为漂移**：SQLite 有 999 参数钳制、MySQL 无（宽表 × 大批次可超 max_allowed_packet）；命令对象 SQLite/MySQL 每行新建 vs PG 复用；三份异常保留清理 helper 各自为政。复制已产生不一致，非"该不该抽象"的取舍题 | `src/PalORM.Sqlite/SqliteProvider.cs:71-244`、`src/PalORM.MySql/MySqlProvider.cs:70-238` | ✅ | 三实现逐行对照 |
| ITM-305 | **SqlTemplateEmitter 双缺陷且零测试**：① 生成非 partial 的固定类名 `SqlTemplates`，同命名空间第二个 `[SqlTemplate]` 即 CS0101；② SQL 提取疑似含边界引号（逐字符推演，[推断]） | `src/PalORM.SourceGen/SqlTemplateEmitter.cs:52-72` | ✅/⚠ | 生成语义流；② 无消费样本可对照 |

### P1（前轮遗留，当前 HEAD 仍未修）

| ID | 发现 | 状态 |
|:--:|------|------|
| ITM-201 | MySQL TEXT 列索引 DDL 报 1170 不可执行（ADR-B 核心交付在 MySQL 上首次迁移即抛异常） | ⬜ 未修（行动项账本确认） |
| ITM-202 | ADR-A 列序校验未覆盖 GridReader/StoredProcBuilder，同型列错位仍静默换数据 | ⬜ 未修 |

### P2

| ID | 发现 | 位置 | 可信度 |
|:--:|------|------|:--:|
| ITM-306 | ToPageAsync 在已有 OrderBy 的查询上生成双 ORDER BY 非法 SQL；公共 OrderBy 二次调用同样触发（无守卫） | `QueryBuilderExtensions.cs:143`、`QueryBuilder.cs:429-431` | ✅ |
| ITM-307 | 用户复合条件不加括号，`a = {x} OR b = {y}` 使 AND 优先级令软删/租户过滤对 OR 分支失效（WhereIn 已有括号纪律，未推广到 Where/OrWhere/Having/CountAsync） | `DataSession.cs:598-600`、`QueryBuilder.cs:77-89` | ✅ |
| ITM-308 | 查询缓存"快照"仅浅拷贝 List，实体实例跨调用方共享可变——QUERY-001 同类逃逸下移到元素层，注释宣称与实现不符 | `QueryBuilderExtensions.cs:15-16,54` | ✅ |
| ITM-309 | IQueryInterceptor 只覆盖 ToListAsync 族，INSERT/UPDATE/DELETE/Bulk/存储过程/多结果集全部不经过——审计场景静默覆盖缺口，与 AddInterceptor 文档不符 | `QueryBuilderExtensions.cs:47-62` | ✅ |
| ITM-310 | ExecuteWithResilience 把命令超时判为可重试且无幂等性约束——超时 ≠ 未执行，包裹 InsertAsync 超时场景双写 [推断] | `Resilience.cs:55-75` | ⚠ |
| ITM-311 | 无 `[Key]` 实体仍生成畸形 SQL（`DELETE FROM t WHERE `），唯一防线 PALORM001 可被 .editorconfig 降级；复合主键静默错误（同一 key 绑所有参数、SetId 只看第一个 PK）且无拒绝诊断 | `CommandFactoryEmitter.cs:70-95,199-205` | ✅ |
| ITM-312 | DDL NOT NULL 只由 `[Required]` 驱动、RowFactory IsDBNull 守卫由 C# 可空注解驱动——两语义源不一致：非空注解无 [Required] 的列建成可空，外部写 NULL 后物化抛异常 | `MigrationEmitter.cs:105`、`RowFactoryEmitter.cs:43-49` | ✅ |
| ITM-313 | .sql 文件内容不在增量键内，只改 .sql 不触发重新生成，嵌入陈旧 SQL 直到 rebuild；风险未文档化 | `SqlFileEmitter.cs:69`、`PalORMGenerator.cs:61-66` | ✅ |
| ITM-314 | 唯一键冲突无统一异常转换，调用方须分别 catch 三驱动异常——业务代码与具体驱动耦合，削弱"一套代码三个库"卖点 | `IDbProvider.cs:61` | ✅ |
| ITM-315 | SQLite 用魔法数字比对 DbOptions 默认值判断"未配置池"——Core 改默认值即全炸；显式 `WithPool(100,30,60)` 被误放行；三 Provider 行为不对称未声明 | `SqliteProvider.cs:18` | ✅ |
| ITM-316 | 稳定版 2.0.1 依赖预览版驱动 + net11.0 单目标 + NU5104 无声压制（.NET 11 GA 前所有消费者被锁死在预览 SDK） | `Directory.Packages.props:9`、三 Provider csproj | ✅ |
| ITM-317 | 未显式调用 SQLitePCL 初始化，NativeAOT/裁剪下自动 bundle 探测不保证存活——恰是 AOT 卖点上最脆的一环 [推断] | `src/PalORM.Sqlite/` | ⚠ |
| ITM-318 | PG Binary COPY 依赖 `parameter.NpgsqlDbType` 推断：可空列（Value=DBNull 无法推断）与 UTC DateTime→TIMESTAMP 列类型错配风险 [推断]，无集成测试证实/证伪 | `PostgreSqlProvider.cs:143-149` | ⚠ |
| ITM-319 | StoredProcBuilder 测试为恒真断言（builder 链式返回 this 后 IsNotNull）——参数绑定/输出参数/GetOutputValue 实际零行为覆盖，README 却有完整示例；全仓 IsNotNull 弱断言 16 处 | `test/PalORM.Integration.Tests/AdvancedTests.cs:21-27` | ✅ |
| ITM-320 | `dotnet test` 对 TUnit/MTP 项目静默零输出——按惯例执行该命令得到无失败信号的空结果，假验证陷阱，README/CI 未提示需 `dotnet run` | test/*/csproj | ✅ |
| ITM-321 | PALORM003/004 描述符标题与实际触发条件错位（按 ID 查文档南辕北辙）；分析器每 [Table] 类型全程序集遍历 O(N²)；char 列生成 `r.GetChar` 主流驱动抛 NotSupportedException [推断] | `PalORMAnalyzer.cs:22-28,188`、`RowFactoryEmitter.cs:101` | ✅/⚠ |

### P3（代表项，完整清单见四流原始报告）

| ID | 发现 | 位置 |
|:--:|------|------|
| ITM-322 | ValueStringBuilder 注释宣称 stackalloc 实为 ArrayPool，Span 构造器死代码；"小查询零分配"声明未实现 | `ValueStringBuilder.cs:7-21` |
| ITM-323 | 日志链路断裂：ILogger 公共入口不可达（CreateAsync 不传），`MinimumLogLevel` 全仓零消费者，死配置误导用户 | `DataSession.cs:25,62`、`DbOptions.cs:91` |
| ITM-324 | 软删幂等不一致：单条 DeleteAsync 缺 `AND deleted_at IS NULL`，重复删除刷新时间戳且返回 1（Bulk 返回 0） | `DataSession.cs:269` vs `DataSession_Bulk.cs:59-61` |
| ITM-325 | Insert 回填不对称：PG/SQLite RETURNING 返回新实例不回填传入实体、MySQL setId 原地回填；BulkMergeAsync 丢弃返回值加剧 | `DataSession.cs:166-188` |
| ITM-326 | CURRENT_TIMESTAMP 时区语义三库不同（SQLite=UTC、MySQL/PG=会话时区）未声明，软删 deleted_at 跨库含义漂移 | 三 Provider |
| ITM-327 | TestDb.FromRows 注释称"真实 RowFactory 物化避免假绿"，实现是原样返回——恰造成其警告的假绿 | `src/PalORM.Testing/TestDb.cs:36-38` |
| ITM-328 | 无 Verify 快照基线（ITM-139 在案未做），obj 陈旧样本与当前 Emitter 已现可观测漂移；生成类型缺 [GeneratedCode] 特性 | test/PalORM.SourceGen.Tests |
| ITM-329 | CacheStore 进程级静态单例跨测试共享，靠 key 前缀而非机制隔离，Clear() 可清掉并行测试条目 | `CacheStore.cs:85-99` |
| ITM-330 | WithTransaction 提交前等待在飞操作无 5 分钟兜底（会话 DisposeAsync 有），被放弃枚举器可致 commit 无诊断挂起 | `SessionOperationState.cs:166-169` |
| ITM-331 | "实现接口即可新增 Provider、零耦合"文档承诺不成立：SqlDialect 封闭枚举 + 三字段 record + QueryBuilder 硬编码分支，新增 SqlServer 需改 Core/SourceGen 约 6-8 处；两个 [Obsolete] 死成员仍 static abstract 强制实现 | `IDbProvider.cs:9`、`SqlDialect.cs:4` 等 |
| ITM-332 | QueryMultipleAsync 挂在 QueryBuilder 上却丢弃全部已构建子句，Where 静默无效；仓库根残留 0 字节 mydatabase.db | `QueryBuilderExtensions.cs:164-185` |

---

## 段 4：优先级判定（危害 × 复杂度）

| ID | 危害 | 复杂度 | 优先级 | 理由 |
|:--:|:--:|:--:|:--:|------|
| ITM-301 | 高 | 易 | P0 | 白名单类型直接编译失败；一行改 GetFieldValue + 全类型回归实体即修 |
| ITM-302 | 高 | 中 | P1 | 多租户数据泄露；与软删过滤统一为 GetDefaultFilterCondition |
| ITM-303 | 高 | 易 | P1 | 金额静默截断，数据正确性；发显式精度或 [Precision] 注解 |
| ITM-304 | 中 | 中 | P1 | 漂移已发生且含 ITM-304 内的 MySQL 无钳制实害；骨架上提一次修三问题 |
| ITM-305 | 中 | 易 | P1 | 特性公开可用但第二次使用即编译失败 |
| ITM-306/307/308 | 中 | 易 | P2 | 框架自身产出非法 SQL/错误语义/共享可变，修法均为局部 |
| ITM-310/317/318 | 中 | 中 | P2 | 均为 [推断]，先补集成测试证实或证伪再修 |
| ITM-316/331 | 中 | 难 | 评估 | 依赖策略与方言扩展模型需 ADR 裁决（见段 7） |

---

## 段 5：方法论自省

**工具选择**：结构发现 = review-snapshot.sh + Glob/Bash；代码审查 = 四路 Task 子代理并行全量逐行（serena 本会话不可用，按回退链执行）；交叉验证 = 双流独立发现比对（ITM-302 双源）+ 编译探针（ITM-301）+ 脚本实测（gate/verify/doc-consistency/Core.Tests）。

**已知局限**：真库行为（MySQL/PG）与 AOT 发布未实测；Integration/SourceGen 测试未实际运行；并发结论为静态推演。

---

## 段 6：跨报告一致性

- 前序报告：`review-2026-07-18-70fc6ec.md`（REQUEST_CHANGES，2 P1 + 5 P2 + 6 P3）
- 未解决项：ITM-201、ITM-202（P1）及 ITM-203~213 全部 ⬜——本轮基线即前序评审提交（9e23c90），零整改间隔，属预期
- 新发现对比：本轮为全量轮（前轮为增量整改验证轮），新增 1 P0 + 4 P1 集中在前轮盲区——SourceGen 白名单类型端到端（ITM-301/305）、多租户消费链路（ITM-302）、Provider 间对称性（ITM-303/304）。前轮"SqlFileEmitter 大改段未逐行复审"的盲区声明与本轮 ITM-313 的发现位置吻合，盲区声明机制有效
- 无回退：前轮已修 12 项回归面本轮未见复发迹象

---

## 段 7：建议与修复方案

### P0 — 紧急
| ID | 修复方案 | 预估 |
|:--:|---------|:--:|
| ITM-301 | `r.GetFieldValue<TimeSpan>()` 替换 GetTimeSpan；新增含全部 PALORM016 白名单类型的"全类型实体"生成+编译+物化回归测试（同时覆盖 ITM-321 的 char） | 0.5 天 |

### P1 — 本迭代
| ID | 修复方案 | 预估 |
|:--:|---------|:--:|
| ITM-302 | GetSoftDeleteCondition 泛化为 GetDefaultFilterCondition（软删 + 参数化 tenant_id），接入全部直连入口；补 SoftDelete+TenantAware 同体实体测试 | 1 天 |
| ITM-303 | MigrationEmitter 默认发 DECIMAL(18,6) 或引入 [Precision]；MySQL 生成物断言 | 0.5 天 |
| ITM-304 | 多值 INSERT 骨架上提 Core 内部 helper（参数化 maxParams + Quote/CreateParameter 委托），PG COPY 保持独立**不并入**；MySQL 补钳制 | 1 天 |
| ITM-305 | partial class + 方法名去重；补 FormattableString.Format 断言测试 | 0.5 天 |
| ITM-201/202 | 按前轮行动项执行（VARCHAR(255)/前缀索引；ValidateColumnOrder 下沉共享） | 1 天 |

### P2 — 下迭代
ITM-306（OrderBy 前缀守卫）、307（用户条件括号包裹，推广 WhereIn 纪律）、308（至少修正注释+文档警示）、309（文档明确覆盖面）、310（文档"仅限幂等"+ retryOnTimeout 选项）、311（CanGenerateEntity 加 [Key] 守卫 + 复合主键拒绝诊断）、312（NOT NULL 取 IsRequired||!IsNullable）、313（文档标注 rebuild 要求）、314（IsUniqueViolation static virtual）、315（WithPool 显式标记位）、319（StoredProcBuilder 行为断言）、320（README/CI 标注 dotnet run）、317/318（先补 AOT/COPY 集成测试）。

### 评估 — 需 ADR
| ID | 需要决策 |
|:--:|---------|
| ITM-316 | 多目标 net10.0;net11.0（LTS）还是坚持 net11.0 单目标 + 声明预览状态 |
| ITM-331 | 方言扩展模型：如实收窄文档承诺（推荐，符合 YAGNI）还是把方言 SQL 收敛为 Provider 携带的描述对象 |
| ITM-108（在案） | 凭据历史重写 + 数据库轮换（用户决策，继续挂账） |

---

## 段 8：多维度综合评价与自我局限声明

### 十维度评分（10 分制，证据见段 3 对应 ITM）

| 维度 | 分 | 依据 |
|------|:--:|------|
| 可维护性 | 7.5 | 注释大量解释"为什么"并引外部案例（GORM #7437、QUERY-001）是典范；扣分：Bulk 三重复制已漂移（304）、软删条件三辅助函数分散滋生 302/307、WithTransaction 双重载 50 行复制 |
| 健壮性 | 7 | 资源生命周期是全库最强项：所有权转移、主异常保留三处一致、放弃枚举器有界等待、熔断半开单探针有不变式论证；扣分：租户穿透（302）、双 ORDER BY（306）、OR 穿透（307）、非幂等重试（310） |
| 可读性 | 8 | 命名清晰、方法长度承载真实复杂度而非缺拆分、partial 拆分方向对；DataSession.cs 1193 行偏大但可控 |
| 可扩展性 | 6 | 接口层好（static virtual 最小实现面、ConfigureSchemaCommand 吸收三库差异）；方言层封闭枚举 + 文档承诺落空（331）、死成员强制实现 |
| 灵活性 | 6.5 | 拦截器/缓存/重试/租户四大扩展点齐备但覆盖面均有缺口（309/308/310/302）——"有此能力"与"能力覆盖完整"之间的差距是本轮主题 |
| 简洁性 | 8 | 无投机抽象：PG COPY/IsTransient/PRAGMA 不强行统一是正确判断；欠抽象仅 304 一处且有漂移实证——本评审明确不以行数论优劣，304 的修复理由是"复制已出错"而非"行数多" |
| 合理性 | 7.5 | MySQL 方言内嵌 Core 有显式守卫是有意识取舍（缓解到位）；SQLite 魔法数字（315）、日志死配置（323）不合理 |
| 兼容性 | 6 | AOT 选型正确（MySqlConnector/Npgsql/源生成绑定/零反射 grep 实证）；预览依赖锁死消费者（316）、SQLitePCL 初始化悬疑（317）、CURRENT_TIMESTAMP 跨库语义（326） |
| 可复用性 | 6.5 | Core 零外部依赖、接口粒度符合 ISP；三 Provider 200 行复制（304）、Testing 包 FromRows 名实不符（327） |
| 可测试性 | 7 | Core 137/137 实测、断言强项精确到异常链引用相等与遥测脱敏、文档口径全部机器守护（8 项自动校验）；扣分：恒真断言（319）、4 个 Core 类型零定向覆盖、无 Verify 快照（328）、dotnet test 静默陷阱（320） |

**综合：7.0/10 · REQUEST_CHANGES**。依赖方向完全符合 Clean Architecture（Core 零 PackageReference、Provider 依赖抽象、方向 grep 实证），资源/错误/SQL 安全/AOT 四项工程纪律高于平均；扣分集中在"编译期承诺与运行时消费的一致性"（302/301/312）与"三实现对称性"（303/304/315/326）两条主线上——这恰是插件-Provider + 源生成架构的固有风险面，建议后续审计轮持续把"生成语义流"与"对称性对照表"作为常设视角。

**我确认**：
- [x] 已运行 scripts/review-snapshot.sh 并粘贴输出
- [x] 已明确声明评审范围和不评审范围
- [x] 已声明抽样策略
- [x] 所有 P0/P1 发现至少 2 个独立信息源验证（ITM-301 编译探针+双路径；ITM-302 双流独立发现；ITM-303/304/305 逐行对照+文档化行为）
- [x] 已排除 *.g.cs 风格评审（仅验语义）
- [x] 盲区已在段 1 声明

**我已知的局限**：真实 MySQL/PG 行为未验证、AOT publish 未重新执行、性能基准未运行、Integration/SourceGen 测试套件未实际运行、并发发现未压测。
