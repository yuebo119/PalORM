# PalORM 全面评审报告：45c476b（深检引擎 v1 首轮全量）

> 评审日期：2026-07-18
> 评审范围：src/ 全部 6 项目 + test/ 9 项目 + scripts/ + docs 口径 + CI
> 评审方法：深检引擎 + review profile（全量档）——四路并行联合流（Core 运行时 / SourceGen 生成语义 / 三 Provider 方言 / 测试与基建）全量逐行；机械防线先行
> 评审基线：`45c476b68332a827cee7dc052abc2be3cc410151`（dev）
> 结论：**REQUEST_CHANGES** — 5 项 P1（其中 3 项探针实证：OrWhere 过滤绕过 ×2 + SQLite 错误码误判；1 项跨租户批量删除；1 项防线自身盲区）+ 14 项 P2 + 12 项 P3。上一轮机械化的四件套防线全部在位且通过，但本轮同时发现防线自身的 5 处结构性弱点——防线也是代码，也要被评审。

---

## 段 1：范围与方法

**评审范围**：
- src/PalORM.Core（23 文件）逐行；src/PalORM.SourceGen（13 文件）逐行 + 快照交叉验证 + Runtime 键集核对；src/{Sqlite,MySql,PostgreSql,Testing} 全量 + IDbProvider 全成员三方对称性对照表
- test/ 新增测试基建（SnapshotTests/DialectSymmetryTests/ArchitectureInvariantTests/QueryBuilderPropertyTests）自身质量专项；scripts/ 门禁绕过路径抽查（G9/G12/G20/G22/G24/G25）；docs 口径算术；CI 结构
- 探针：3 次（CORE-01/02 DryRun SQL 实证、PROV-01 真驱动错误码实测）+ 1 次 NuGet 官方 XML 文档核验（MySqlConnector 池参数单位）

**明确不评审**：`*.g.cs` 风格（仅验语义）；bench/；tools/PalORM.Scaffold；G1-G11/G13-G19/G21/G23 逐项绕过分析（仅抽查 6 项）

**抽样策略**：src 全量逐行；scripts 抽查；docs 口径抽样

**盲区声明**：
- 驱动物化行为（byte/char/DateOnly/TimeOnly 在 Npgsql/MySqlConnector 上的转换）未真库穷尽；MySQL 1586 错误码未真库触发
- AsyncLocal 事务在 ExecutionContext 分叉下的行为未穷尽推演；并发结论为静态推演未压测
- PgNotificationListener 断线重连未做故障注入；多程序集片段合并只读代码未实测

---

## 段 2：评审基线

```
时间：2026-07-18 22:16 CST · 分支：dev · 提交：45c476b · 工作树：干净
SDK：11.0.100-preview.6.26359.118
机械验证实测（评审前全绿）：build 0W0E · gate-check 25/25 · verify-ai-system 11/11 ·
doc-consistency 8/8 · stub-check PASS · assertion-strength 19/19 ·
SourceGen.Tests 80/80 · Core.Tests 147/147 · Integration.Tests 146/146（真库）
```

---

## 段 3：准确性分析（发现清单）

### P1（本迭代必修）

| ID | 发现 | 位置 | 可信度 | 信息源 |
|:--:|------|------|:--:|--------|
| ITM-401 | **OrWhere 默认过滤绕过（两变体，探针实证）**：① 租户过滤经 `builder.Where(...)` 注入、未计入 `_defaultWhereCount`，TenantAware 实体首个 `OrWhere` 生成 `WHERE ("tenant_id" = @p0) OR (user)` — 跨租户读泄漏；② `默认过滤 + Where(A) + OrWhere(B)` 生成 `WHERE d AND (A) OR (B)` ≡ `(d AND A) OR B` — B 分支逃逸软删/租户过滤。逐子句前缀方案根治不了，需把用户 WHERE 子句整体括组（`d AND ((A) OR (B))`） | `DataSession.cs:138-143`、`QueryBuilder.cs:96-106` | ✅ | DryRun SQL 探针双实证 + SQL 优先级[事实] |
| ITM-402 | **`[Timestamp]` 注解即用即坏**：列被排除出 INSERT（IsInsertable=false），DDL 却生成 `NOT NULL` 且无 DEFAULT — MigrateAsync 建表后该实体每次 INSERT 必触发 NOT NULL 约束失败；PALORM017 清单不含 [Timestamp]，编译期零告警。快照实体未覆盖此注解（防线盲区实证） | `MigrationEmitter.cs:106-119` × `TableModel.cs:186-190` | ✅ | 代码路径闭环 + PALORM017 触发点枚举双源 |
| ITM-403 | **SQLite IsUniqueViolation 主码 19 过宽（真驱动探针实证）**：UNIQUE(2067)/NOT NULL(1299)/FK(787)/PK(1555) 全部主码 19 — NOT NULL/FK 违规被误报为唯一冲突，用户"记录已存在"分支吞掉数据完整性错误。MySQL/PG 精确判定，差异无方言依据，违反 ITM-314 统一契约 | `SqliteProvider.cs:75-76` | ✅ | Microsoft.Data.Sqlite 真驱动探针实测错误码 |
| ITM-404 | **BulkDeleteAsync 无租户过滤**：软删/物理删两路径均无 `tenant_id` 条件，TenantAware 实体可按主键跨租户批量删除；单条 DeleteAsync 有过滤，同语义不同覆盖，判定为遗漏（与 ITM-302 同根因）。BulkUpdate/BulkMerge/Seed 同文件待逐一核对 | `DataSession_Bulk.cs:20-60` | ✅ | 方法体全读 + 单条路径对照双源 |
| ITM-405 | **架构不变式防线对 partial 文件失明**：ArchitectureInvariantTests 只读 DataSession.cs，DataSession_Bulk.cs 的 5 个公共触表入口（BulkInsert/BulkDelete/BulkUpdate/BulkMerge/Seed）既不在登记表也不被反向守卫扫描——ITM-404 正是从这个盲区漏出的 | `ArchitectureInvariantTests.cs:73-99` | ✅ | 源码 + grep Bulk 文件 0 处过滤 token 双源 |

### P2（下迭代）

| ID | 发现 | 位置 | 可信度 |
|:--:|------|------|:--:|
| ITM-406 | WithCache + FirstOrDefault/Single 家族把 `_take=1/2` 截断列表写入缓存键，后续同键 ToListAsync 命中截断数据静默丢行 | `QueryBuilderExtensions.cs:50-55,85-107` | ⚠ |
| ITM-407 | Skip-only 查询非 MySQL 方言输出裸 `OFFSET n`——SQLite 语法要求 OFFSET 伴随 LIMIT（应 `LIMIT -1 OFFSET n`），产出非法 SQL | `QueryBuilder.cs:675-691` | ⚠[事实:SQLite 文法] |
| ITM-408 | 空表/全过滤时 SUM/AVG `Convert.ToDecimal(DBNull)` 抛 InvalidCastException；Max/Min 已处理，不对称 | `DataSession.cs:659-663,683-687` | ⚠ |
| ITM-409 | 两属性映射同一列名（重复 [Column]）无诊断，生成 `INSERT INTO t (x, x)` 运行期才炸 | `PalORMAnalyzer.cs`（缺检查） | ✅ |
| ITM-410 | SqlFileEmitter 错误路径生成物本身 CS1010（字符串字面量跨行）——设计好的 Obsolete 诊断机制被架空 | `SqlFileEmitter.cs:129` | ✅ |
| ITM-411 | SqlTemplate SQL 提取三重脆弱：方法前注释含 `$"` 取错内容；`\"` 转义被当闭合引号截断；插值洞引用方法参数生成 CS0103 | `SqlTemplateEmitter.cs:60-73` | ✅ |
| ITM-412 | PG COPY probe 校验段 + 异常保留清理骨架与 Core 骨架逐字复制且 Dispose 签名已开始分叉——ITM-304 同型温床（COPY 主体是真方言差异，合规；骨架不是） | `PostgreSqlProvider.cs:82-103,197-217` | ✅ |
| ITM-413 | ArchitectureInvariantTests.ExtractMethodBody 以 `\n    }` 定界：被检方法重构为表达式体时吞并后续方法体致假通过；注释含 "GetDefaultFilter" 字样同样满足断言 | `ArchitectureInvariantTests.cs:103-113` | ✅ |
| ITM-414 | QueryBuilderPropertyTests LCG 低位退化：奇偶严格交替使 `Next(2)` 恒 0/1 交替——DefaultFilter 专项 100 种子全部只走 Where/OrWhere 交替序列，"连续两次 OrWhere"零覆盖 | `QueryBuilderPropertyTests.cs:206-217` | ✅[数学事实] |
| ITM-415 | SnapshotTests 更新模式（PALORM_UPDATE_SNAPSHOTS=1）静默全绿无警示，CI 无该变量防护——shell 残留导出使本地快照防线整体关闭而不自知 | `SnapshotTests.cs:108-144` | ✅ |
| ITM-416 | DialectSymmetryTests 引用符归一化为整串删除——某方言漏引用标识符时三方言归一化后仍一致，对"引用符存在性"失明；且未覆盖 Upsert SQL（方言差异最大语句） | `DialectSymmetryTests.cs:35-37` | ✅ |
| ITM-417 | 门禁绕过路径三联：G20 不匹配 `.GetAwaiter().GetResult()`；G22 不含 `[UnconditionalSuppressMessage]`；G9 整行豁免含 localhost 的行（`Password=x;Host=localhost` 被放行） | `gate-check.sh:89,125,127` | ✅ |
| ITM-418 | **文档口径算术不一致**：分项 147+80+146=373，总计与 README 徽章写 361/361（上轮遗留）；doc-consistency 只查跨文档相等不查加和——机械校验盲区实证 | `docs/架构设计.md:276`、`README.md:5` | ✅[算术] |
| ITM-419 | AOT 三宿主覆盖不等价：SQLite 宿主验证 MigrateAsync（源生成 DDL）+ 跨程序集注册合并，PG/MySQL 宿主两者皆无（手写 DDL）——CreateTableSqlByDialect 的 PG/MySQL 产物在 AOT 原生路径零真库验证（E1 残留敞口） | `test/PalORM.AotTest.{Pg,MySql}/Program.cs` | ✅ |

### P3（代表项）

| ID | 发现 | 位置 |
|:--:|------|------|
| ITM-420 | PALORM003 只扫本程序集表名，跨程序集 FK（Runtime 明确支持片段合并）被 Error 级拦死 | `PalORMAnalyzer.cs:283-288` [推断:需产品裁决] |
| ITM-421 | [Computed] 表达式原样注入三方言 DDL 无方言分支/诊断；快照无 [Computed] 实体 | `MigrationEmitter.cs:110-112` |
| ITM-422 | 可空主键 `[Key] long? Id` 通过全部校验，SQLite/PG 对 NULL PK 行为分叉无诊断 | `SourceGenerationValidation.cs:37-41` |
| ITM-423 | MinimumLogLevel 仍是死配置（ITM-323 只接通了 LoggerFactory，级别过滤零消费点） | `DbOptions.cs:103` |
| ITM-424 | StoredProcBuilder.MarkExecuted 在 Enter 门禁前置位——门禁拒绝后 builder 永久假"已执行" | `StoredProcBuilder.cs:89-91` |
| ITM-425 | GroupBy 列名不带表限定（其余子句均限定），JOIN 下 ambiguous column | `QueryBuilder.cs:224-229` |
| ITM-426 | assertion-strength-check.sh 块切分：尾随辅助方法计入块内、方法名含 Throws 即满足匹配——漏报方向 | `assertion-strength-check.sh:36-43` |
| ITM-427 | test-quality-scripts.sh 夹具就地 sed 真实文档，异常中断留损坏态；真实计数恰为 71/71 时恒等替换假失败 | `test-quality-scripts.sh:156-165` |
| ITM-428 | TestDb 环境变量缺省回退系统库（postgres/mysql），DROP/CREATE 测试表进系统库 | `TestDb.cs:23,32` |
| ITM-429 | 生成类型缺 [GeneratedCode] 特性（覆盖率/analyzer 工具链噪音）；RequiredMember 等 3 个 polyfill 死代码 | 各 Emitter、`Polyfills.cs` |
| ITM-430 | GetLimitOffsetClause 死代码不对称（仅 MySQL 实现已 Obsolete 成员）；QuoteQualifiedIdentifier 全死路径 | `MySqlProvider.cs:37-43` 等 |
| ITM-431 | EnterTransactionOperation 无事务流时抛 ObjectDisposedException——类型误导诊断 | `SessionOperationState.cs:75-80` |

---

## 段 4：优先级判定（危害 × 复杂度）

| ID | 危害 | 复杂度 | 优先级 | 理由 |
|:--:|:--:|:--:|:--:|------|
| ITM-401 | 高 | 中 | P1 | 跨租户读泄漏 + 软删行复活；需用户子句括组重构，非局部补丁 |
| ITM-404 | 高 | 易 | P1 | 跨租户批量删除是数据破坏；修法同单条路径，直接可抄 |
| ITM-402 | 高 | 易 | P1 | 注解公开可用但即用即坏；DDL 补 DEFAULT 或 PALORM017 扩清单 |
| ITM-403 | 中 | 易 | P1 | 一行修复（SqliteExtendedErrorCode 2067/1555）+ 测试改真库触发 |
| ITM-405 | 中 | 易 | P1 | 防线盲区不修，同类缺陷继续从此漏出；扫描范围扩至 DataSession*.cs |
| ITM-406/407/408 | 中 | 易 | P2 | 框架自产错误数据/非法 SQL，修法局部 |
| ITM-415/416/413/414 | 中 | 易-中 | P2 | 防线自身弱点——防线失明比业务缺陷更贵 |
| ITM-418 | 低 | 易 | P2 | 口径算术 + doc-consistency 补加和校验（下沉） |
| ITM-419 | 中 | 中 | P2 | PG/MySQL AOT 宿主补 MigrateAsync 路径 |
| ITM-420 | 中 | — | 评估 | 多程序集 FK 是否支持场景需产品裁决（ADR） |

---

## 段 5：方法论自省

**工具选择**：机械防线先行（快照/对称性/架构/性质/门禁全绿后才开人工深检）；四路 Task 子代理并行全量逐行；[推断] 零容忍执行——3 项 P1 当轮探针实证（DryRun SQL ×2、真驱动错误码 ×1），1 项官方文档核验证伪（MySqlConnector 池参数单位盲区消除）。

**已知局限**：见段 1 盲区声明。会话中断一次（四代理经转录恢复续跑，结论完整性未受损——恢复后各代理补齐了收尾项）。

---

## 段 6：跨报告一致性

- 前序报告：`review-2026-07-18-9e23c90-full.md`（25 项整改完成）
- 回归核对：前轮修复项零复发（ITM-314/315/317/318/325/326/327 逐项复核在位）；但 ITM-314 的统一契约在 SQLite 侧本就未真正成立（ITM-403 实证主码过宽——前轮验证用手工构造异常，未触发真驱动扩展码，属**验证方法缺陷**而非回退）
- 复发判定：ITM-401/404 与 ITM-302/307 同根因（"过滤覆盖断裂"），计入复发 ×2——上轮修复覆盖了直连入口与括号纪律，未覆盖 OrWhere 前缀语义与 Bulk 文件
- 防线有效性双面证据：正面——快照/对称性/性质测试使本轮 SourceGen 零 P0（上轮 1 P0），性质测试上轮当场抓住 OrWhere 首缺陷；反面——本轮 5 项 P2 是防线自身弱点，防线代码需与业务代码同标准评审

---

## 段 7：建议与修复方案

### P1 — 本迭代
| ID | 修复方案 | 预估 |
|:--:|---------|:--:|
| ITM-401 | 用户 WHERE 子句整体括组：默认过滤与用户组间恒 AND（`d AND ((A) OR (B))`）；租户过滤改走 AddDefaultFilter 统一计数；补 TenantIsolation OrWhere 用例 + 性质测试改逐动作校验 | 1 天 |
| ITM-404 | BulkDelete/BulkUpdate/BulkMerge 补租户条件（抄单条路径 tenantFilter）+ Bulk 租户隔离测试 | 0.5 天 |
| ITM-405 | ArchitectureInvariantTests 扫描 `DataSession*.cs` 全部 partial + Bulk 5 入口登记 | 0.5 天 |
| ITM-402 | MigrateAsync DDL 为 [Timestamp] 列发 DEFAULT CURRENT_TIMESTAMP（三方言）或 PALORM017 扩清单告警；快照实体补 [Timestamp] | 0.5 天 |
| ITM-403 | `{ SqliteErrorCode: 19, SqliteExtendedErrorCode: 2067 or 1555 }`；测试改 :memory: 真库触发 | 0.5 天 |

### P2 — 下迭代
ITM-406（First 族绕缓存或独立键）、407（`LIMIT -1 OFFSET`）、408（Sum/Avg 对齐 Max/Min）、409（列名唯一性诊断 PALORM021）、410/411（SqlFile/Template 修 + 负向测试）、412（骨架上提收口）、413（Roslyn 语法树替代正则）、414（LCG 换 xorshift/PCG + 分布断言）、415（更新模式打印警示 + CI 置空变量）、416（引用符存在性专项断言 + Upsert 对称面）、417（三门禁模式补齐）、418（口径修正 + doc-consistency 加和校验）、419（PG/MySQL AOT 宿主接 MigrateAsync）。

### 评估 — 需 ADR
| ID | 需要决策 |
|:--:|---------|
| ITM-420 | 多程序集实体布局是否官方支持场景（支持→PALORM003 改跨片段查询；不支持→文档声明限制） |

---

## 段 8：质量指标与自我局限声明

### 四指标（同步 .ai/metrics.md）

| 指标 | 本轮值 | 说明 |
|------|--------|------|
| 发现数 | P0: 0 · P1: 5 · P2: 14 · P3: 12 | 零 P0 为防线机械化后首次（上轮 1 P0 属快照防线现覆盖类别） |
| 探针数 | DryRun SQL 2 · 真驱动 1 · 官方文档核验 1（证伪盲区） | P1 全部探针实证或双源，零 [推断] 残留 |
| 逃逸 | 0 新增（E1/E2 无复发） | ITM-403 属前轮验证方法缺陷，计为复发面不计逃逸 |
| 复发 | 2（ITM-401/404 ← "过滤覆盖断裂"根因类）+ 1 验证方法缺陷（ITM-403 ← ITM-314） | 根因类账本已更新 |

**我确认**：
- [x] 已运行 scripts/review-snapshot.sh 并粘贴输出
- [x] 已明确声明评审范围和不评审范围
- [x] 已声明抽样策略
- [x] 所有 P1 发现至少 2 个独立信息源验证（3 项探针实证 + 2 项双源交叉）
- [x] 所有 P1 发现无 [推断] 残留
- [x] 已排除 *.g.cs 源生成器生成文件
- [x] 盲区已在段 1 声明
- [x] 已追加 .ai/metrics.md 轮次行

**我已知的局限**：驱动物化行为未真库穷尽、并发未压测、断线注入未做、多程序集合并未实测、G 门禁仅抽查 6 项。
