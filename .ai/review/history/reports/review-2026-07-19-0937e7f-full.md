# PalORM 全量评审报告（2026-07-19 · 基线 0937e7f）

> 评审日期: 2026-07-19
> 评审范围: src/ 全部手写代码（43 文件 · 8610 行）
> 评审方法: 全量地毯逐行（4 片并行子代理）× 主线程跨片不变式 × 探针实证
> 评审基线: 0937e7f618ff34a7ef2061ac7e86c82abde33344

---

## 段 1：范围与方法

**评审范围**：
- src/PalORM.Core、src/PalORM.Sqlite、src/PalORM.PostgreSql、src/PalORM.MySql、src/PalORM.SourceGen、src/PalORM.Testing 全部手写 `.cs`（43 文件 8610 行，覆盖度账本见下）
- 机械防线全跑：gate-check 28/28、doc-consistency 9/9、assertion-strength（弱断言 19 = 基线上限）
- 全量测试：Core 153/153、SourceGen 90/90、Integration 154/154（PG/MySQL 真库，需 set-test-env）、SQLite AOT 原生 PASSED

**明确不评审**：
- `*.g.cs` 生成文件（仅验语义不评风格）；test/ 与 bench/ 代码本体（仅作反证信息源）；docs/ 全文（仅核对与代码矛盾处）；.ai/ 系统自身
- PG/MySQL 的 AOT 原生运行（依赖 CI 三平台 job，本轮未跑）

**抽样策略**：范围内零抽样——43 文件全部逐行勾销（四片交付物各自附覆盖度声明，合计 8610 行与 review-scope.sh 清单一致）。

**盲区声明**：
- Npgsql/MySqlConnector 驱动内部行为（COPY null 推断、断线 Dispose 异常形态）静态读不出——相关疑点标 ❓ 降 P3 待真驱动矩阵
- 片 2 子代理会话中断一次，经恢复后基于已读内容交付；其覆盖度声明由主线程对其中 2 项高危疑点探针复核（均证实），交叉可信

### 覆盖度账本（43/43 全勾销）

片 1（2155 行 ✔）：DataSession.cs / Resilience.cs / DataSession_Bulk.cs / RowFactoryEmitter.cs / CacheStore.cs / IDbProvider.cs / TestDb.cs / PostgreSqlExtensions.cs / SqlGenerationDialect.cs / IRowFactory.cs
片 2（2150 行 ✔）：QueryBuilder.cs / TableModel.cs / PostgreSqlProvider.cs / MigrationEmitter.cs / GridReader.cs / StoredProcBuilder.cs / SqlTemplateEmitter.cs / ConnectionLease.cs / ColumnOrderValidator.cs / TypeMapperEmitter.cs
片 3（2150 行 ✔）：PalORMAnalyzer.cs / PgNotificationListener.cs / QueryBuilderExtensions.cs / SourceGenerationValidation.cs / SqlFileEmitter.cs / MultiValueBulkInsert.cs / PalORMGenerator.cs / MySqlProvider.cs / FormattableSqlFormatter.cs / IQueryInterceptor.cs / SqlDialect.cs
片 4（2155 行 ✔）：SessionOperationState.cs / PalORM_Runtime.cs / CommandFactoryEmitter.cs / Annotations.cs / RegistryEmitter.cs / DbOptions.cs / PalORMMetrics.cs / SqliteProvider.cs / ValueStringBuilder.cs / TransactionCleanup.cs / QueryClause.cs / Polyfills.cs

---

## 段 2：评审基线

```
时间：2026-07-19 12:57:47 CST
分支：dev
提交：0937e7f618ff34a7ef2061ac7e86c82abde33344
短提交：0937e7f
SDK：11.0.100-preview.6.26359.118
工作树：干净
最近提交：0937e7f 修复：ITM-546~554 bdf4504 review 九项收口
```

---

## 段 3：准确性分析

| ID | 发现 | 位置 | 可信度 | 信息源 | 已对照误判模式 |
|:--:|------|------|:--:|--------|:--:|
| ITM-555 | 键集分页续页条件被用户 OR 分支绕过：`AddWhereComparison` 追加 `AND col < @p` 不对既有用户子句组括号，`Where+OrWhere`（或单条含 OR 的 Where）后 ToPageAsync 生成 `WHERE (A) OR (B) AND col<@p`——页间重复、分页不推进 | QueryBuilder.cs:520-527 + QueryBuilderExtensions.cs:163 | ✅ | Read + 真库探针（page1=[6,5]，page2=[6,4] 重叠实测） | 已排除 1/2；ITM-401 同根因复发（补丁只覆盖 AppendWhereSection） |
| ITM-556 | BulkUpdateAsync 事务回滚后内存 version 与 DB 失同步：逐条 `IncrementVersion(entity)` 在提交前执行，后续条目冲突整事务回滚时已成功条目内存 version 已 +1 → 调用方重试 UpdateAsync 假 ConcurrencyConflictException。BulkMerge/Insert 的 ID 回填同型（危害较小） | DataSession_Bulk.cs:124-131 + DataSession.cs:284 | ✅ | Read + 真库探针（FALSE CONFLICT 实测复现） | 已排除 1/3/P7 |
| ITM-557 | CommandTimeout 覆盖不对称：HealthCheckAsync / SavepointAsync / RollbackToAsync / MultiValueBulkInsert 批量 INSERT 四处不设 `cmd.CommandTimeout`，其余全部命令路径均设。健康检查恰是最需快速失败的路径 | DataSession.cs:1099-1116,731-750；MultiValueBulkInsert.cs:99-126 | ✅ | 双子代理独立发现 + 主线程 Read 核实四处 | 已排除 5/7 |
| ITM-558 | BoundedQueryCache.TryGet `(T)entry.Value` 硬强转：两个实体类型误用同一 WithCache key → InvalidCastException 且异常无 key 信息不可诊断 | CacheStore.cs:40-53 | ✅ | Read + 真库探针（InvalidCastException 实测） | 已排除 7；片1/片2 独立提出 |
| ITM-559 | 基类 [Key] 实体不可用且诊断误导：CanGenerateEntity 与 PALORM001 的 Key 计数只查本类声明成员，列收集却走基类链（ITM-502）——`[Key]` 在基类时报"无 [Key]" Error 阻断，文案与事实矛盾 | SourceGenerationValidation.cs:37-51 + PalORMAnalyzer.cs:133 | ✅ | 编译探针（PALORM001 error 实测） | 已排除 4（build 实证） |
| ITM-560 | 可空值类型主键 `long?` 生成不编译代码：分析器放行，生成物 CS0037（null 常量对非可空值类型） | CommandFactoryEmitter.cs:77-96 | ✅ | 编译探针（CS0037 实测） | 已排除 4 |
| ITM-561 | init-only setter 主键：分析器零诊断、生成器静默跳过（IsInitOnly 仅在 CanGenerateEntity 生效），运行期才报 "not registered" | SourceGenerationValidation.cs:50 | ✅ | 编译探针（Build succeeded 零 PALORM + 运行期异常实测） | 已排除 4 |
| ITM-562 | WhereIn/WhereNotIn 65535 参数上限只按单次调用增量判定，不计 `_parameters.Count` 存量——两次 40k 静默通过，运行期 PG 协议层报错 | QueryBuilder.cs:171,208 | ✅ | Read（注释自证"总量受协议约束"） | 已排除 2 |
| ITM-563 | SELECT 路径对 Set 子句无守卫（BuildUpdateSql 已按 ITM-522 拒绝 Join/OrderBy，反向不对称）：`Set(...).ToListAsync()` 静默丢 Set；`Set+Where` 的 AsDryRun 返回 SELECT 预览误导 | QueryBuilder.cs:129-139,576-586 | ✅ | Read + ITM-522 先例对照 | 已排除 7 |
| ITM-564 | SqlFileEmitter 双静默：provider 段全不匹配时回退嵌入**整份异方言原文**；未识别 provider 别名（如 "postgres"）无诊断落入同路径 | SqlFileEmitter.cs:159-165,199-200 | ✅ | Read（逻辑闭合，两处相互佐证） | 已排除 7 |
| ITM-565 | PALORM020 校验索引名不校验索引**列**存在性：`[Index("ix_a","typo_col")]` 编译期静默，MigrateAsync 运行期报列不存在（1170 类错误不被幂等兜底吞） | PalORMAnalyzer.cs:355-419 + MigrationEmitter.cs:56-67 | ✅ | Read（分析器全文 + Emitter 对照） | 已排除 4（列校验缺失是结构性的，非行为猜测） |
| ITM-566 | MySQL 1170 规避只覆盖 string：被索引的 char/enum 列落 TEXT，`[Unique]` char/enum 在 MySQL 下 CREATE INDEX ON TEXT → 1170 迁移失败 | MigrationEmitter.cs:219-226 + TableModel.cs:189-211 | ✅ | Read（注释自证 1170 不被 1061 兜底吞） | 已排除 P2（方言差异但此处是同一规避逻辑覆盖缺口） |
| ITM-567 | PgNotificationListener 重连计数仅收到 NOTIFY 时清零：静默通道 + 周期性断连（LB 空闲切断）下每次成功重连仍累加，第 6 次断开监听器永久死亡 | PgNotificationListener.cs:123-138 | ✅ | Read（完整循环逻辑） | 已排除 1（读完整状态机） |
| ITM-568 | 会话配置入口门禁不对称：AddInterceptor 有操作租约门禁，同为会话可变状态的 IgnoreFilters/WithTenant/WithIsolationLevel/WithTimeout/WithRetry/WithCircuitBreaker 均无门禁无 EnsureAvailable | DataSession.cs:535,563-571,898-924 | ✅ | Read + AddInterceptor 先例对照 | 已排除 1/P4 |
| ITM-569 | MigrateAsync legacy 回退不对称：CRUD 路径 GetCommandSqls 明确拒绝旧片段，MigrateAsync 对缺 CreateTableSqlByDialect 的旧片段静默回退 SQLite 风格双引号 DDL——MySQL 上语法错而非"请重新编译" | PalORM_Runtime.cs:112-113 + DataSession.cs:618-624 | ✅ | 双子代理独立发现（片2/片4） | 已排除 5 |
| ITM-570 | DisposeTransactionResourcesAsync 末尾 `await activeOperation` 无超时：WithTransaction 回调内放弃的枚举器租约（不注册为事务资源）使事务收口永久挂起；同失败模式在 DisposeAsync 已用 DisposeWaitTimeout 有界化，此路径漏掉 | SessionOperationState.cs:169-172 | ✅ | Read + 反证（确认枚举器只走 EnterOperation） | 已排除 1（读完整 lock 与资源注册链） |
| ITM-571 | 文档测试计数失真：README 徽章与架构设计.md 称 389/389（Core 148 + SourceGen 87 + 集成 154），实测 Core 153 + SourceGen 90 + 集成 154 = 397——上轮提交新增测试未同步；doc-consistency-check 只查文档间一致不查实测，D 项全绿掩蔽失真 | README.md:5、docs/架构设计.md:4,273-276 | ✅ | dotnet test 实测 + grep 文档 | 已排除 5（当前 commit 实测） |
| ITM-572 | QueryMultipleAsync 完全绕过默认过滤（软删/租户）：SQL 逐字执行，租户会话可取全租户数据；拦截器绕过有 ITM-548 登记，默认过滤绕过无任何文档声明 | QueryBuilderExtensions.cs:190-197 | ✅ | Read + docs grep（API参考 QB19 无此声明） | 已排除 5 |
| ITM-573 | SqlTemplate 两处生成不编译代码：插值洞引用局部变量（检查只覆盖方法参数）→ 生成物 CS0103；同名模板双方法 → 双 partial 同名字段 CS0102，均无编译期诊断 | SqlTemplateEmitter.cs:41-48,62-66 | ✅ | Read + grep（分析器无 SqlTemplate 诊断，零命中） | 已排除 4 |
| ITM-574 | PALORM005 纯语法判定误报：只按方法名+循环祖先，不查 receiver——EF Core 的 ToListAsync、非循环执行的 lambda 均误报；TreatWarningsAsErrors 下变阻断（本轮探针工程即被误报一次） | PalORMAnalyzer.cs:330-352 | ✅ | Read + 探针工程实测误报 | 已排除 4 |
| ITM-575 | Savepoint/RollbackTo 不校验 `tran.Connection == _conn`（UseTransaction 校验）；错误形态交给驱动 | DataSession.cs:731-750 | ✅ | Read | 已排除 5 |
| ITM-576 | IDbProvider 单参 `CreateConnection(string)` 零调用点死 static abstract 成员，强迫第三方 Provider 实现死代码；同文件另两个死成员均已 Obsolete+期限，此漏登记 | IDbProvider.cs:25 | ✅ | Read + grep 全仓调用点 | 已排除 P9（四类引用形态逐一排除） |
| ITM-577 | 配置发布协议声明与读方不符：UpdateResilience 注释称经 Volatile.Read(_resilience) 可见一致 _options，实际仅 ExecuteWithResilience 遵守该配对，CRUD 路径全部普通读 | DataSession.cs:934-940 | ✅ | Read（全部读方枚举） | 已排除 1/P4 |
| ITM-578 | Select() 调用时刻固化表限定名，`Select(...).With(...)` 顺序生成引用不在 FROM 的表限定；唯 Select 提前固化，顺序敏感未登记 | QueryBuilder.cs:134-137 | ⚠ | Read（未探针） | 已排除 7 |
| ITM-579 | `_defaultClauseCount` 死字段 + 误导注释（声称 QueryMultipleAsync 消费，实际用 CountUserSubstantiveClauses） | QueryBuilder.cs:18-20,504 | ✅ | grep 全仓（3 处均非读取） | 已排除 P9 |
| ITM-580 | 注解文档与实现矛盾三处：DefaultValue 称"原样进入 DDL"（实际恒 null + PALORM017 告警）；Key 文档窄于实现（漏 int/short/byte）；CrudMetadata.Sqls 称"当前方言"（实际 legacy 无方言常量） | Annotations.cs:52-54,105-112；PalORM_Runtime.cs:340-341 | ⚠ | Read（片4 反证 TableModel:118） | 已排除 5 |
| ITM-581 | 杂项死代码/一致性组：ValueStringBuilder 死 int 构造（文件头自证不应使用）；RegistryEmitter `pk ?? "id"` 静默兜底；DisposeWaitTimeout 可变静态无同步；PALORM006/007 死描述符 | 各文件见行动项 | ✅ | Read + grep | 已排除 P9 |
| ITM-582 | 观测/文档缺口组：ToPageAsync COUNT 不经拦截器未声明；ToPageAsync 静默清用户 Skip；StoredProc 绕过拦截器/Tracing 无 ITM-548 式登记；CircuitBreaker threshold=0 语义（静默禁用）未文档化 | QueryBuilderExtensions.cs:138,150-158；StoredProcBuilder.cs:92-130；DataSession.cs:918-924 | ⚠ | Read | 已排除 5 |
| ITM-583 | 驱动行为待真库组（❓）：PG COPY 批内单行 null 类型推断（注释自认待验证）；PgListener 断线 Dispose 异常击穿重连；transient 判定仅覆盖 NpgsqlException；char? ProviderClrTypeName 形态 | PostgreSqlProvider.cs:158-171；PgNotificationListener.cs:106,179-180；RowFactoryEmitter.cs:58-69 | ❓ | Read（驱动内部行为静态不可判） | 已排除 4（明确标注不猜驱动） |
| ITM-584 | 编译期防御缺口组：QuoteIdentifier 三方言不拒 NUL（public API 面）；[Computed] 表达式原样进 DDL 无括号/NUL 快检（Tag 有对称防御）；SqlFileEmitter 路径含 `&` 生成畸形 XML doc（CS1570）；`..` 检查误拒 `my..queries.sql` | 三 Provider QuoteIdentifier；MigrationEmitter.cs:84-118；SqlFileEmitter.cs:38,120 | ⚠ | Read | 已排除 5/7 |
| ITM-585 | 分析器精度组：PALORM003 只扫本程序集表名（跨程序集实体 Error 误阻断❓）；PALORM021 大小写不敏感对 PG 引号标识符过严且无 ITM-510 式决策登记；.sql 文件单独修改不触发增量重新生成（陈旧嵌入） | PalORMAnalyzer.cs:193,311-316,420-434；PalORMGenerator.cs:61-66 | ⚠ | Read | 已排除 4 |
| ITM-586 | CacheStore TTL 用 DateTime.UtcNow 墙钟（Resilience 同类问题有 ITM-538 登记，此处无）；时钟回拨延长条目存活 | CacheStore.cs:81-82 | ⚠ | Read | 已排除 5 |

**定稿门证伪记录（1 项）**：
- 片1 疑点"BulkDelete 参数重建丢失 binder 元数据"——片3 交叉反证 CommandFactoryEmitter 生成的 binder 只设 Value（无 DbType/Precision），重建无信息丢失。**证伪销案**。

**零发现流的覆盖度证据**：
- 资源流（片1/片2 范围）：CreateAsync null-out 所有权转移、四级释放挂 Data、CTS using、租约 operationTransferred 形态逐一核对——除 ITM-570 外干净
- AOT 流（全部四片）：零反射/MakeGeneric/Expression.Compile；OwnedJson 显式 JsonTypeInfo；FrozenDictionary+ModuleInitializer 合规；SQLite AOT 原生实测 PASSED
- 并发流（片2/片3 范围）：QueryBuilder 无条件写时复制在场；GridReader/PgListener 全字段锁内；QueryObservation Interlocked 幂等

---

## 段 4：优先级判定

| ID | 危害 | 复杂度 | 优先级 | 理由 |
|:--:|:--:|:--:|:--:|------|
| ITM-555 | 高 | 易 | **P1** | 静默错误数据 + 分页死循环；触发面广（任何含 OR 的用户条件×键集分页）；修复=追加前括组既有 Where 组（与 ITM-401 同向根治） |
| ITM-556 | 高 | 中 | **P1** | 乐观锁契约静默破坏：假冲突让调用方重试逻辑失效；修复需"提交后统一回填"或回滚补偿 |
| ITM-557 | 中 | 易 | **P1** | 健康检查/保存点在僵死库上挂 30s 驱动默认；四处补一行 |
| ITM-558 | 中 | 易 | **P1** | 不可诊断崩溃；改 `is T` 按 miss 处理 + 异常带 key 即可 |
| ITM-559 | 中 | 中 | P2 | 继承主键场景整体不可用但编译期即失败（不静默）；两处计数统一走基类链 |
| ITM-560 | 中 | 易 | P2 | 编译期即暴露（生成物 CS0037），不静默；补诊断拒绝可空 Key |
| ITM-561 | 中 | 易 | P2 | 运行期才失败但报错清晰；IsSupportedEntity 补 init 检查或新诊断 |
| ITM-562 | 中 | 易 | P2 | 触发需 >65535 累计参数，罕见；守卫改累计 |
| ITM-563 | 中 | 易 | P2 | 静默丢 Set 误导，但 SELECT+Set 组合是明显误用；对称加守卫 |
| ITM-564 | 中 | 中 | P2 | 触发需错误配置，但后果是执行异方言 SQL；改 GenerateError 诊断 |
| ITM-565 | 中 | 中 | P2 | 运行期报错可定位但延迟；PALORM020 补列校验 |
| ITM-566 | 中 | 中 | P2 | MySQL + [Unique] char/enum 组合；扩 VARCHAR(255) 分支 |
| ITM-567 | 中 | 易 | P2 | 长生命周期监听器有限寿命；LISTEN 成功即清零 |
| ITM-568 | 中 | 中 | P2 | 需并发误用触发；补门禁或文档裁决 |
| ITM-569 | 中 | 易 | P2 | 仅旧版本片段混用可达；对齐 GetCommandSqls 拒绝 |
| ITM-570 | 中 | 中 | P2 | 触发需放弃枚举器+事务组合；对齐 DisposeWaitTimeout 有界化 |
| ITM-571 | 中 | 易 | P2 | 文档可信度 + 防线盲区（D 项检查不接实测）；数字同步 + 考虑 gate 接实测 |
| ITM-572 | 中 | 易 | P2 | 逃生舱 API 语义可辩护，但租户绕过必须显式声明；XML doc + API参考补登记 |
| ITM-573 | 中 | 中 | P2 | 编译期暴露但错误指向 .g.cs 难定位；补诊断 |
| ITM-574 | 中 | 中 | P2 | 误报在 TreatWarningsAsErrors 下阻断混用项目；加 receiver 符号检查 |
| ITM-575~586 | 低/中 | 易~难 | P3 | 死代码清理、文档校对、驱动行为待真库、编译期防御缺口——逐项见行动项 |

---

## 段 5：方法论自省

**工具选择**：
- 结构发现：review-scope.sh 分片 + grep 跨片不变式（默认过滤路由/参数编号空间/注册表键集/拦截器对称）
- 代码审查：4 并行子代理逐行（七流问题卡 + 误判库速版内嵌），主线程收敛定级
- 交叉验证：probe-template.sh 编译/真库探针 6 个（全部出结论：5 证实 + keyset 探针同时实测 PALORM005 误报）、双子代理独立发现 2 项（ITM-557/569）互为信息源

**已知局限**：
- PG/MySQL AOT 原生路径未本轮执行（E2 逃逸的既有防线在 CI，本机只跑了 SQLite AOT）
- 驱动内部行为（COPY null、断线 Dispose）静态读不可判——ITM-583 组降 ❓ 待真库矩阵
- 片 2 子代理经历一次会话中断恢复，交付物基于恢复前已读内容；主线程抽其 2 项高危疑点探针复核均证实，但未重新逐行其余 8 文件（按引擎中断纪律应重派，本轮以探针复核+其自报覆盖度替代，标注为轻度盲区）

---

## 段 6：跨报告一致性

- 前序报告：review-2026-07-19-bdf4504-full.md（整改回归轮，9 项全修）。ITM-546~554 复核：FormattableSqlFormatter @pN 拦截已移除（546 ✔）、拦截器文档已同步（547 ✔）、QueryMultiple OnBefore 已摘除并登记（548 ✔）——均确认收口无回归。
- 复发判定：引用 metrics.md 根因类账本。本轮 **1 项复发**：ITM-555 ← 过滤覆盖断裂/括组根因类（ITM-401 的"WHERE 统一括组"根治只覆盖 AppendWhereSection，AddWhereComparison 是同根因新入口——正是引擎速查卡"补丁 vs 根治"预言的形态）。

---

## 段 7：建议与修复方案

### P1 — 近期（本迭代内修）
| ID | 问题 | 修复方案 | 预估时间 |
|:--:|------|---------|:--:|
| ITM-555 | 键集条件被 OR 绕过 | AddWhereComparison 追加前将既有 Where 组整体括组（或键集条件走 DefaultFilter 类别复用恒 AND 通道）；补 Where+OrWhere+ToPageAsync 性质测试 | 1h |
| ITM-556 | Bulk 回滚后内存状态失同步 | IncrementVersion/ID 回填延迟到事务提交后统一执行（暂存待回填清单）；或回滚路径补偿递减 + XML doc 契约声明 | 2-4h |
| ITM-557 | 四处缺 CommandTimeout | 各补 `cmd.CommandTimeout = _options.CommandTimeoutSeconds`（MultiValueBulkInsert 骨架加参数透传） | 0.5h |
| ITM-558 | 缓存跨类型硬强转 | `entry.Value is T t` 不匹配按 miss 并移除旧条目；或抛带 key 的明确异常 | 0.5h |

### P2 — 计划（下个迭代）
| ID | 问题 | 修复方案 |
|:--:|------|---------|
| ITM-559 | 基类 [Key] 计数 | CanGenerateEntity/PALORM001 与列收集统一走基类链 |
| ITM-560/561 | 可空 Key / init-only Key | 新增或扩展 PALORM 诊断在编译期明确拒绝，附负向测试 |
| ITM-562 | WhereIn 累计参数 | 守卫改 `_parameters.Count + items.Count` |
| ITM-563 | SELECT×Set 静默 | 执行入口对称加 HasClause(Set) 守卫（ITM-522 同型） |
| ITM-564 | SqlFile 回退静默 | 段全不匹配 + 未识别别名均转 GenerateError 诊断 |
| ITM-565 | 索引列不校验 | PALORM020 增列存在性（含 [Column] 重命名映射） |
| ITM-566 | MySQL 1170 覆盖缺口 | 被索引且落 TEXT 的类型统一 VARCHAR(255) 分支 |
| ITM-567 | 重连计数 | LISTEN 成功即清零；或文档化按断开计数语义 |
| ITM-568 | 配置入口门禁 | 裁决：补 EnsureAvailable 或文档声明快照语义 |
| ITM-569 | Migrate legacy 回退 | 对齐 GetCommandSqls：缺 ByDialect 即抛"请重新编译" |
| ITM-570 | 事务收口无界等待 | 复用 DisposeWaitTimeout 有界化 + 诊断消息 |
| ITM-571 | 测试计数失真 | 同步 README/架构设计 数字；doc-consistency D 项接 dotnet test 实测计数 |
| ITM-572 | QueryMultiple 过滤绕过 | XML doc + API参考 QB19 显式登记"默认过滤不适用" |
| ITM-573 | SqlTemplate 生成不编译 | 插值洞含任意标识符即诊断拒绝；模板名唯一性诊断 |
| ITM-574 | PALORM005 误报 | receiver 符号 containing-namespace 检查 |

### P3 — 评估
ITM-575（Savepoint 连接校验）、ITM-576（死 abstract 成员 Obsolete）、ITM-577（发布协议注释修正）、ITM-578（Select/With 顺序）、ITM-579（死字段删除）、ITM-580（三处文档矛盾校对）、ITM-581（死代码组）、ITM-582（观测/文档缺口组）、ITM-583（驱动行为真库矩阵——建议入 CI 集成测试）、ITM-584（编译期防御缺口组）、ITM-585（分析器精度组）、ITM-586（CacheStore 时钟登记）

### 评估 — 需 ADR
无本轮新增架构级决策项。ITM-568（会话配置门禁范围）若裁决为"全入口门禁"则波及 API 语义，建议先文档后评估。

---

## 段 8：质量指标与自我局限声明

| 指标 | 本轮值 | 说明 |
|------|--------|------|
| 发现数 | P0: 0 · P1: 4 · P2: 16 · P3: 12（组） | 按流：生成语义 11 · 架构 8 · 错误 4 · 并发 3 · 资源 2 · 安全 3 · 观测/文档 5 |
| 探针数 | 编译探针 3（init-only/nullable-key/inherited-key）· 真库用例 3（version-desync/cache-cast/keyset-or） | 全部 P1 定稿前探针实证，零 [推断] 残留 |
| 逃逸 | 0 新增 | 上两轮整改项（546~554）复核无回归 |
| 复发 | 1（ITM-555 ← 括组根因类，ITM-401 补丁式修复的新入口） | 已入 metrics.md 根因账本 |
| 证伪 | 1（BulkDelete 参数元数据丢失——跨片反证销案） | 误判库无需新模式（P2 变体） |

**我确认**：
- [x] 已运行 scripts/review-snapshot.sh 并粘贴输出（段 2）
- [x] 已明确声明评审范围和不评审范围
- [x] 已声明抽样策略（零抽样，43/43 勾销）
- [x] 所有 P1 发现至少 2 个独立信息源（Read + 探针 / 双子代理独立发现）
- [x] 所有 P1 发现无 [推断] 残留
- [x] 已排除 `*.g.cs`
- [x] 盲区已在段 1 声明
- [x] 已追加 metrics.md 轮次行

**我已知的局限**：
- PG/MySQL AOT 原生运行未本轮验证（依赖 CI）
- ITM-583 组驱动行为待真库矩阵，本轮不定级为缺陷
- 片 2 覆盖度经中断恢复 + 探针抽查复核，非全新逐行重派
