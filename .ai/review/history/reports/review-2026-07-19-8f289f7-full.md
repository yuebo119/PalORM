# PalORM 全面 Review 报告：8f289f7（review 系统 v3.1 · 全量档 · 首次并行地毯实战）

> 日期：2026-07-19 · 基线：`8f289f7`（dev，工作树干净）
> 方法：deep-check-engine v2.2 全量档——`review-scope.sh` 分 4 片按行数均衡（各 ~2070 行），4 个子代理各领一片**地毯式逐行**（带七流问题卡+误判库速版），主线程做跨片全局不变式 + 统一定级 + 探针实证 + 定稿门三问。
> 结论：**REQUEST_CHANGES** — 2 P0 + 9 P1 + 20 P2 + 14 P3。全量 8268 行 43 文件逐行覆盖率 100%（账本见段 1）；11 项探针实证，1 项证伪。

---

## 段 1：范围与方法 + 覆盖度账本

**范围**：src/ 全部手写代码 43 文件 8268 行，逐文件逐行，零抽样。`*.g.cs` 仅验语义不评风格。
**明确不检查**：test/、bench/、tools/、docs/ 正文（口径由 doc-consistency 守护）。
**分片覆盖账本**（每片子代理逐文件 wc -l 核对，全部勾销）：
- 片1（2076 行 9 文件）：DataSession/DataSession_Bulk/StoredProcBuilder/CacheStore/ValueStringBuilder/SqlFileEmitter/TypeMapperEmitter/TestDb/SqlGenerationDialect — ✅
- 片2（2066 行 11 文件）：QueryBuilder/QueryBuilderExtensions/TableModel/Resilience/DbOptions/RowFactoryEmitter/IDbProvider/SqlTemplateEmitter/PostgreSqlExtensions/QueryClause/Polyfills — ✅
- 片3（2067 行 12 文件）：PalORMAnalyzer/PgNotificationListener/CommandFactoryEmitter/RegistryEmitter/MigrationEmitter/MultiValueBulkInsert/PalORMMetrics/MySqlProvider/ConnectionLease/ColumnOrderValidator/SqlDialect/IRowFactory — ✅
- 片4（2059 行 11 文件）：SessionOperationState/PalORM_Runtime/Annotations/PostgreSqlProvider/SourceGenerationValidation/GridReader/PalORMGenerator/SqliteProvider/FormattableSqlFormatter/IQueryInterceptor/TransactionCleanup — ✅

**盲区**：并发疑点（P1-并发-01/02）为静态推演+单机探针，未压测/未故障注入；PG COPY 全 null 列、MySQL 大小写索引等 5 项需真库矩阵（本机已跑 SQLite 探针，PG/MySQL 标"待 CI 服务容器证实"）。

## 段 2：评审基线

```
时间：2026-07-19 02:27 CST · 分支 dev · 提交 8f289f7 · 工作树干净
SDK 11.0.100-preview.6.26359.118
机械防线（review 前全绿）：gate 27/27 · Core 148/148 · SourceGen 87/87 · Integration 154/154（真库）
```

## 段 3：准确性分析（发现清单 + 定稿门对照）

### P0（合并前必修）

| ID | 发现 | 位置 | 可信度 | 信息源 | 已对照误判模式 |
|:--:|------|------|:--:|--------|:--:|
| ITM-501 | **亚秒 CommandTimeout 静默变永不超时**：`(int)TimeSpan.TotalSeconds` 截断——`CommandTimeout=500ms → 0`，而 ADO.NET CommandTimeout=0 语义是**无限等待**。超时保护对所有 <1s 配置翻转为反面（全 16 处命令超时赋值 + QueryBuilder 直连路径无 Resilience 兜底） | DataSession.cs 全部 `(int)…TotalSeconds`、QueryBuilderExtensions.cs:45 | ✅ | 探针实证（(int)0.5=0） | 已排除 3/P4 |
| ITM-502 | **基类属性列静默丢失**：`typeSymbol.GetMembers()` 不含继承成员——实体继承 `AuditBase{CreatedAt}` 时基类映射列不进 ColumnNames/INSERT/SELECT，数据静默丢失，无诊断 | TableModel.cs:40 | ✅ | 探针实证（T2 列集缺 created_by） | 已排除 5/P1 |

### P1（本迭代必修）

| ID | 发现 | 位置 | 可信度 | 信息源 | 已对照模式 |
|:--:|------|------|:--:|--------|:--:|
| ITM-503 | **SaveAsync 绕过乐观锁**：UPSERT 分支无条件 `DO UPDATE SET col=excluded.col`，不带 `[ConcurrencyCheck]` WHERE、不 IncrementVersion——UpdateAsync 强制乐观锁而 SaveAsync 对同实体静默 last-write-wins，两写入口语义不对称且无文档 | DataSession.cs:411-459 | ✅ | 探针实证（stale 覆盖 Version=0） | 已排除 6 |
| ITM-504 | **init-only [Key] 生成物 CS8852**：SourceGenerationValidation 接受 init-only setter，但 SetId 生成 `entity.Id=id` → 编译错误落在 .g.cs，用户难定位 | SourceGenerationValidation.cs:18-26 × CommandFactoryEmitter.cs | ✅ | build 探针（CS8852 实证） | 已排除 4/P4 |
| ITM-505 | **BindDelete/SetId 的 EndsWith 类型判定误伤无符号主键**：`"ulong".EndsWith("long")` 为 true → uint/ulong/nint 主键生成 `(long)key` 拆箱，运行期 InvalidCastException（拆箱须精确类型） | CommandFactoryEmitter.cs:81-84,139 | ⚠[推断] | Read 双源；需 uint 主键探针（修复前补） | 已排除 P5 |
| ITM-506 | **熔断器对确定性异常也计数**：唯一约束冲突/SQL 语法错误等非瞬时失败同样 RecordFinalFailure——正常并发插入连续 5 次唯一冲突即熔断整个执行器，阻断全部后续操作 | Resilience.cs:88-92 | ✅ | Read 全路径 + 与 IsUniqueViolation 对照 | 已排除 3 |
| ITM-507 | **熔断器在飞旧失败反复顺延恢复时间**：RecordFinalFailure 无 generation 过滤，熔断打开后旧代慢操作陆续失败，每次重置 `_circuitOpenUntil` 并 generation++，多个慢失败可无限延长熔断窗口 | Resilience.cs:130-148 | ✅ | Read 完整状态转换（误判库 1：读完整块） | 已排除 1 |
| ITM-508 | **QueryAsyncEnumerable 放弃枚举器泄漏操作租约**：迭代器租约在 Dispose 才释放；手写 MoveNext 中途 break 不 DisposeAsync → 租约永不退出，会话后续操作全被门禁拒绝，DisposeAsync 挂起至 5min 超时 | DataSession.cs:738-761 | ⚠[推断] | Read + 状态机分析；需枚举器探针 | 已排除 P4 |
| ITM-509 | **@pN 手写与生成占位符静默冲突**：FormattableSqlFormatter 生成 `@p{baseIndex+i}` 不检测用户 SQL 文本中字面 `@p0`——混写时同名参数共享绑定值，静默错误数据（非注入，仍参数化） | FormattableSqlFormatter.cs:53 | ✅ | 探针实证（混写 @p0 未抛，静默共享） | 已排除 2 |
| ITM-510 | **MySQL 大小写索引名静默丢失**：PALORM020 用 Ordinal 比较，MySQL 索引名大小写不敏感——`ix_Foo`/`ix_foo` 过分析器，迁移时 1061 被 IsDuplicateSchemaObject 幂等吞掉 → 第二索引永不创建（PALORM020 声称消除的掩蔽在方言差异处复活） | PalORMAnalyzer.cs:362,406 | ⚠[推断] | Read 双源；需 MySQL 真库迁移探针 | 已排除 4/P7 |
| ITM-511 | **DryRun 参数写穿 builder**：AsDryRun 返回内部 DbParameter 本体，改 `.Value` 后复用同一 builder 执行，污染值经 paramFactory 传导到真实绑定 | QueryBuilder.cs:420 × QueryBuilderExtensions.cs:334 | ✅ | 探针实证（改参数后 rows=1 写穿） | 已排除 P6 |

### P2（下迭代，20 项）

| ID | 发现 | 位置 |
|:--:|------|------|
| ITM-512 | 三 Provider/SourceGen 注解匹配全按 `AttributeClass.Name` 不校验命名空间——混挂 EF/DataAnnotations 特性时 PALORM001/003/004/017 误报或误处理 | PalORMAnalyzer.cs:129 等 + TableModel.cs:43 |
| ITM-513 | 拦截器只覆盖 SELECT：ExecuteNonQuery/count/QueryMultiple 不经 IQueryInterceptor，审计/日志观察不到写操作 | QueryBuilderExtensions.cs |
| ITM-514 | WhereIn 参数总量不封顶：文档称规避参数上限，实际只切单 IN 片段；传 66000 值超 PG 65535 协议上限 | QueryBuilder.cs:154 |
| ITM-515 | With(CTE)+Include：JOIN ON 右端限定实体表，FROM 已切 CTE 名 → 实体表不在 FROM，SQL 非法 | QueryBuilder.cs:264,570 |
| ITM-516 | NamingConvention/ApplyNaming 死配置：SnakeCase 静默无效（探针实证列名仍 UserName），未挂 Obsolete | DbOptions.cs:50-68 |
| ITM-517 | DbOptions 无正数校验：CommandTimeout=0/负 MaxPoolSize 经 init 绕过 WithPool 校验 | DbOptions.cs:19 |
| ITM-518 | MySQL decimal 无精度：Length/Precision/Scale 恒 null → MySQL DECIMAL(10,0) 静默截断小数 | TableModel.cs:112 × MigrationEmitter.cs |
| ITM-519 | [Timestamp] DEFAULT CURRENT_TIMESTAMP 无类型约束：TimeOnly/DateOnly 生成 MySQL 非法 DDL，建表期才炸 | MigrationEmitter.cs:124 |
| ITM-520 | char 列读取 `GetString(o)[0]`：TEXT 空串 IndexOutOfRange 无列名上下文 | RowFactoryEmitter.cs:104 |
| ITM-521 | SqlTemplateEmitter 取方法体第一个插值串：SQL 前有日志插值时静默提取错误模板 | SqlTemplateEmitter.cs:34 |
| ITM-522 | ExecuteNonQueryAsync 静默丢弃 Join/OrderBy/Take（仅 CTE 抛）——含 JOIN 的 Set 更新超预期行集 | QueryBuilder.cs:536 |
| ITM-523 | QueryMultipleAsync 守卫把 Tag()（Comment 类别）计入 _clauses → Tag 后误拒 | QueryBuilderExtensions.cs:189 |
| ITM-524 | GetActiveTransaction 对已释放的显式事务静默回退——用户指定事务失效时查询脱离事务执行 | QueryBuilder.cs:432 |
| ITM-525 | ForeignKey 全链死代码：TableModel 收集 + PALORM004 督促 OnDelete，但无 REFERENCES DDL 生成——引导用户配无效属性 | TableModel.cs:97 |
| ITM-526 | PgNotificationListener 重连计数跨断线累积（仅收到通知才归零）：低流量 channel 网络抖动数天即第 6 次断线永久终止 | PgNotificationListener.cs:123 |
| ITM-527 | PG COPY 依赖 NpgsqlDbType 从值推断：全 null 可空列/Local Kind DateTime 类型推断失败或写错 | PostgreSqlProvider.cs:157 |
| ITM-528 | MySQL 1061 兜底把"索引已存在但列集不同"当幂等——改 [Index] 列后旧索引静默残留 | MySqlProvider.cs:64 |
| ITM-529 | RegistryEmitter 位置参数序 Sqlite→PG→MySql 与 SqlDialect 枚举序（PG=0）不同——若任何消费点按序号索引即全错位 | RegistryEmitter.cs:52 |
| ITM-530 | RS1035（源生成器禁 IO）全局 NoWarn 抑制：SqlFileEmitter transform 阶段 File.ReadAllText 非确定构建，应局部 pragma+Justification | csproj:18 × SqlFileEmitter.cs:69 |
| ITM-531 | 兼容 Metrics 入口伪造成功样本：LogQuery/RecordQueryStart/RecordQueryDuration 一律记 outcome=success，一次查询三重计数污染指标 | PalORMMetrics.cs:24 |

### P3（14 项，代表）

| ID | 发现 | 位置 |
|:--:|------|------|
| ITM-532 | WithTenant(null) 不抛——租户过滤静默关闭，失败开放（探针实证） | DataSession.cs:552 |
| ITM-533 | MaxAsync/MinAsync 用无 culture 的 ChangeType（其余聚合用 InvariantCulture）——逗号小数 locale 解析错位 | DataSession.cs:675,683 |
| ITM-534 | DisposeCoreAsync 只释放 IDisposable 拦截器，IAsyncDisposable-only 拦截器泄漏 | DataSession.cs:1148 |
| ITM-535 | QueryContext.Parameters 元素可变 + 明文——拦截器改 Value 影响执行、记录参数绕过 [SensitiveData] 脱敏 | IQueryInterceptor.cs:24 |
| ITM-536 | nullable-disable 上下文实体：string 判非空，生成无 IsDBNull 守卫，DB NULL 运行期 SqlNullValueException | RowFactoryEmitter.cs:44 |
| ITM-537 | Select 投影列不带表限定（GroupBy 已修 ITM-425）——JOIN 下 ambiguous | QueryBuilder.cs:129 |
| ITM-538 | 熔断器用 DateTime.UtcNow 墙钟——时钟回拨延长熔断/前拨提前放行 | Resilience.cs:122 |
| ITM-539 | ValueStringBuilder int 容量构造 + SqlFileEmitter.SanitizeIdentifier 死成员无 Obsolete；IsView/Schema/Database 死模型字段 | 多处 |
| ITM-540 | StoredProcBuilder.GetOutputValue 直接拆箱 (T?)——驱动回填 int 而声明 long 时 InvalidCastException | StoredProcBuilder.cs:83 |
| ITM-541 | MigrationEmitter [Computed] 表达式原文入 DDL 不校验——三方言引号风格差异下同表达式可能仅一种方言合法 | MigrationEmitter.cs:86 |
| ITM-542 | HealthCheck/ValidateSchema 把 ex.Message 透传——驱动异常含主机名/端口，探活端点透传泄露拓扑 | DataSession.cs:583,1087 |
| ITM-543 | ValidateSchemaAsync 未注册类型静默跳过全部校验——原生 SQL 路径校验开关形同虚设 | ColumnOrderValidator.cs:16 |
| ITM-544 | EquatableArray default 实例 GetHashCode/枚举 NRE（当前无触发路径，防御缺口） | TableModel.cs:202 |
| ITM-545 | StoredProcBuilder char 列 + SqlFileEmitter 路径前缀 StartsWith 无尾分隔符（当前被 `..` 拒绝挡住，纵深防御缺口） | 多处 |

## 段 4：优先级判定（危害 × 复杂度）

| ID | 危害 | 复杂度 | 优先级 | 理由 |
|:--:|:--:|:--:|:--:|------|
| ITM-501 | 高 | 易 | P0 | 超时保护对全部亚秒配置反转；一处钳制（<1s 报错或按 ms 设 CommandTimeout）修全部 |
| ITM-502 | 高 | 中 | P0 | 继承实体数据静默丢失是最隐蔽的正确性缺陷；需递归 GetMembers 或发诊断 |
| ITM-503 | 高 | 中 | P1 | 乐观锁被 SaveAsync 静默绕过=并发覆盖；补 UPSERT 的 version 条件或文档声明豁免 |
| ITM-504/505 | 中 | 易 | P1 | 生成物编译错误/运行期拆箱异常；validation 补拒绝 |
| ITM-506/507 | 高 | 中 | P1 | 熔断器误熔断正常业务/无限延长——弹性设施反成故障源 |
| ITM-508/509/511 | 中-高 | 中 | P1 | 租约泄漏挂起会话/静默错误数据/DryRun 写穿 |
| ITM-510 | 中 | 易 | P1 | 方言大小写掩蔽索引丢失，同 ITM-201/403 根因类 |

## 段 5：方法论自省

**首次并行地毯实战数据**：4 片各 ~2070 行子代理并行，墙钟约等于单片时间（而非串行 4×）；总逐行量 8268 行 100% 覆盖，账本可审计。七流问题卡命中显著——P0/P1 的 11 项里 9 项直接由问题卡条目触发（超时截断=错误卡、基类丢列=生成语义卡、SaveAsync 乐观锁=架构卡）。probe-template.sh 使 11 项探针在本轮内完成（超时/基类/乐观锁/CS8852/@pN/CTE/DryRun/命名/租户），符合"探针是定稿要求"。
**定稿门三问执行**：每条 P0-P2 对照误判库（段 3"已对照模式"列）；反证搜索证伪 1 项（片1 的 DataSession.cs:144 租户 FormattableStringFactory 疑似——反证：QuoteIdentifier 输出不含 `{}`，格式串文本段安全，转合规）；可复现表述——P0/P1 全部含触发路径。

## 段 6：跨报告一致性

- 前序：`review-2026-07-18-45c476b.md`（5 P1 全整改）。本轮零复发（ITM-401/402/403/404/405 修复在位，OrWhere 括组/[Timestamp] DDL/SQLite 扩展码/Bulk 过滤/架构防线经交叉验证仍成立）。
- 复发判定引用 metrics 根因类账本：ITM-510（方言大小写索引）与 ITM-201/303/315 同属"三方言不对称"根因类，计入复发面 ×1；ITM-518/519/527/528 同类但为新方言面，计新发现。

## 段 7：建议与修复方案

**P0**：ITM-501 CommandTimeout 改按 TotalMilliseconds 或 <1s 时钳制为 1 + 校验；ITM-502 TableModel 递归收集基类映射属性或对有基类映射属性的实体发诊断。
**P1**：ITM-503 UPSERT 补 version 条件或 XML doc 声明；ITM-504/505 validation 拒绝 init-only [Key] 与无符号主键；ITM-506 熔断仅对 `_isTransient` 为真的失败计数；ITM-507 失败带 generation；ITM-509 Format 扫描原文 `@p\d+` 冲突即抛；ITM-510 索引名 MySQL 侧按不敏感比较。
**下沉**：ITM-501→gate 正则（`(int).*TotalSeconds` 禁用）；ITM-502→SourceGen 快照补继承实体；ITM-504/505→AnalyzerDiagnosticsTests 负向；ITM-510→DialectSymmetryTests 大小写用例。

## 段 8：质量指标与自我局限

| 指标 | 本轮值 |
|------|--------|
| 发现数 | P0:2 · P1:9 · P2:20 · P3:14 |
| 探针数 | 编译探针 1（CS8852）· 真库/运行探针 9（超时/基类/乐观锁/@pN/CTE/DryRun/命名/租户/... SQLite）· 待 CI 5（PG COPY/MySQL 索引等真库矩阵） |
| 逃逸 | 0 新增 |
| 复发 | 1（ITM-510 ← 三方言不对称根因类） |
| **证伪** | **1**（DataSession.cs:144 租户格式串疑点经反证搜索转合规） |

**局限**：并发 6 项静态推演未压测；PG COPY/MySQL 索引/decimal 精度 5 项待 CI 服务容器真库矩阵；EF 特性混挂误报（ITM-512）未构造真实 EF 实体 build 验证（标 ⚠）。
